# cs-bot-aion 运行时依赖(生产/容器)
# 说明:
#   numpy     — csapp/kb.py 顶层 import,必需
#   fastembed — 在线向量检索(e5 模型推理)用;不装则自动退化为关键词 FAQ
# DeepSeek LLM 走标准库 urllib 直连 API,无需额外依赖。
#
# fastembed 精确锁定 0.8.0:仓库内置 kb/*/vectors 索引即由该版本(mean pooling)
# 生成,升版可能改变池化/嵌入行为导致与索引失配,检索质量静默劣化。
numpy>=1.24
fastembed==0.8.0
