# 泰文 CMap 还原层 · 执行报告

> 日期:2026-09-01 · 状态:**已完成并验证,已固化进 kb/THA/docs** · 关联设计:v0.3 §5.5 / Phase 0 F1

## 一、确认的问题(证据)

- 源:泰文车主手册内嵌字体 **AngsanaNew**(含 Bold),ToUnicode CMap 把若干泰文组合符号错误映射成 ASCII/控制字符。
- 例:`คู่มือ`→`คู-มือ`(`่`→`-`)、`ชาร์จ`→`ชาร7จ`(`์`→`7`)、`เปิด`→`เปKด`(`ิ`→`K`)。
- 规模:泰文字符间 `-` 退化 5414 处 + `7/K/</P/T/g/Y/B/f/o` 等;受影响 220/221 段。

## 二、还原映射(已用干净语料确证)

干净语料(救援指南/快速指南/配置表)提供判据,确定每对替换关系:

| 错误符号 | 正确泰文 | 判据词 |
|---|---|---|
| `-` | `่` MAI EK (U+0E48) | คู่มือ, อย่าง, จะ |
| `7` | `์` THANTHAKHAT (U+0E4C) | ชาร์จ, ฟังก์ชั่น |
| `K`/`T` | `ิ` SARA I (U+0E34) | เปิด, ปิด |
| `<` | `ั` MAI HAN AKAT (U+0E31) | ฟังก์ชั่น |
| `g`/`P` | `้` MAI THO (U+0E49) | ไฟฟ้า, ป้องกัน |
| `f` | `่` MAI EK | ปุ่ม |
| `#` | `้` MAI THO | ห้าม, ต้อง, ได้ |
| `\x18`/`\x81` | `่` MAI EK | ส่วน, คู่มือ, ปุ่ม |
| `\x11` | `์` THANTHAKHAT | ชาร์จ, รถยนต์, อุปกรณ์ |

> 保留为低置信/不启用(可能涉及真实数字或歧义):`B`、`Y`、`o`、`2`、`4`。

> **注意**:曾把 `์`(THANTHAKHAT)误写为 U+0E4D(实际 NIKHAHIT),正确为 **U+0E4C**——已在验证中修正。

## 三、还原层模块

`tools/thai_restore.py`:
- `CONFIRMED`:高置信映射;`LOW_CONFIDENCE`:可选。
- `restore_segment(text, include_low=False, conservative=True)`:`conservative` 模式只替换**两侧都是泰文字符**的符号,避免误伤真实数字/页码/英文。
- `restore_text()`。

`tools/apply_thai_restore.py`:把还原固化进 `kb/THA/docs`,segment 加 `cmap_restored: true` 标记。

## 四、验证结果

| 指标 | 还原前 | 还原后 |
|---|---|---|
| 可疑字符(泰文之间) | 10014 | **88**(↓99.1%) |
| `ชาร์จ` | 4 | **386** |
| `คู่มือ` | 0 | **30** |
| `เปิด` / `ปิด` | 0 | **476 / 814** |
| `ฟังก์ชั่น` / `ไฟฟ้า` | 0 | **57 / 156** |
| `ป้องกัน` / `ปุ่ม` / `อย่าง` | 0 | **174 / 141 / 301** |

还原后文本示例(通畅泰文):
> การดึงเข็มขัดนิรภัยล่วงหน้า เมื่อเกิดเหตุรถชนกันตัวเข็มขัดนิรภัยแบบดึงกลับจะรัด เข็มขัดนิรภัยให้แน่นอย่างรวดเร็วเพื่อชะลอการเคลื่อนที่เฉื่อย ของผู้...

## 五、应用与后续

- 已固化:`kb/THA/docs/{owner,quickstart,rescue}_*.jsonl` 已用干净文本(owner 220/221 还原,rescue 2,quickstart 1)。
- **下游收益**:泰文知识对 LLM / embedding 都可用。嵌入检索泰文直查 recall@5 由 0.114→0.129(轻微提升);根本瓶颈是 MiniLM 弱检索,与文本损坏无关,需换更强的多语言检索模型(如 multilingual-e5)进一步验证。
- **残留**:88 处(部分为低置信字符与罕见字形),规模极小,可后续微调映射再压低。

## 结论

泰文 CMap 损坏**确实存在且已修复**——映射还原层是首选、便宜、可逆的方案(无需 OCR)。任务完成。
