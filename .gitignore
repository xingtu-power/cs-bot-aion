# 运行态数据(会话/业务库/调试日志)
data/

# 工作区内嵌私人 memory log（不进 git；本机留存）
.workbuddy/memory/

# 工作区内嵌 skill 生成物（zip + 目录为本地产物；SKILL.md 源文件如需共享可手动复制）
.workbuddy/skills/

# 本地 playwright-cli 调试产物（截图/YAML trace/控制台日志）
.playwright-cli/

# 密钥(勿入库;模板见 deploy/.env.example)
.env
deploy/.env

# 本地导出的镜像包(手动上传用)
csbot-dist/

# 内嵌第三方库(需 pip 安装)
tools/pylib/
tools/_lib/

# 模型/依赖缓存(需下载/安装)
tools/emb_cache/
tools/hf_cache/
tools/pipcache/
tools/dsh_home/

# 生成物/系统文件
__pycache__/
*.pyc
*.pyo
.DS_Store
