# 非匿名会话接入设计 · 方案一（宿主身份透传）

> 场景：**AION 官网/App 已有账号体系**（用户已登录）。聊天窗只需**接收宿主传来的 `user_id`**，透传给后端，用于**关联会话、加载档案、跳过重复收集、个人化**。未登录访客仍走匿名。

---

## 0. 身份来源（一机制两用）
| 来源 | 触发 | userId |
|---|---|---|
| **无宿主：自动生成** | 首次访问聊天窗 | 前端 `localStorage` 生成持久 `u_***`（设备/浏览器级匿名身份） |
| **有宿主：真实账号** | 宿主已登录 | 宿主注入 `window.__DSH_USER__.id`（真实 user_id） |

> 二者同一套透传/关联机制：前端 `send()` 带 `userId`；后端 `pipeline(user_id=...)` 关联。未生成/未登录 → userId 为空 → 纯匿名。

### 无宿主：前端自动生成（localStorage）
```js
function getUserId(){
  let uid = localStorage.getItem("cs_bot_uid");
  if(!uid){
    uid = "u_" + (crypto.randomUUID? crypto.randomUUID().replace(/-/g,"") : Math.random().toString(36).slice(2)+Date.now().toString(36));
    localStorage.setItem("cs_bot_uid", uid);
  }
  return uid;
}
const userId = getUserId();   // 跨刷新/会话持久
```
- **界限**：仅同一浏览器/设备；清缓存/换设备 → 新 userId（仍是匿名、无 PII）。

## 1. 思路
```
宿主(已登录)  ──注入──▶  聊天窗  ──userId 透传──▶  后端
  user_id/档案          window.__DSH_USER__      /api/v1/chat
                                                    │
                                              会话.user_id + 档案
                                              (跳过重复收集/个人化)
```
- 宿主侧已有账号与资料 → 聊天窗**不建登录**，只做**身份接收 + 透传**。
- 后端拿到 `user_id` 后：关联会话、查档案、命中后**不重复索要联系方式**。

## 2. 前端改动
**a) 接收宿主身份**（聊天窗页面加载时读）：
```js
const USER = window.__DSH_USER__ || {};   // 宿主注入: {id, name, phone, email, vehicle}
const userId = USER.id || "";             // 未登录为空 → 匿名
```
> 宿主可在页面 `script` 里提前写 `window.__DSH_USER__ = {id:"u123", name:"张三", phone:"13...", email:"...", vehicle:"AION UT"};`

**b) 发送时带 userId**（`send()` 请求体）：
```js
body: JSON.stringify({sessionId, message:q, market, langHint:lang, userId})
```

**c) 个人化展示**（可选）：顶部/问候用 `USER.name`，如"您好，{name}"。

**d) 降级**：`userId` 为空 → 纯匿名，行为不变。

## 3. 后端改动
**a) `/api/v1/chat`**：读 `body.userId` → 传给 `pipeline.chat(..., user_id=...)`。

**b) `pipeline.chat(session_id, message, location, explicit_market, lang_hint, user_id=None)`**：
- 会话恢复后：`if user_id: session.user_id = user_id`。
- **档案注入**：若有 user_id（且宿主提供了档案或后端能查），把 `phone/email/vehicle/name` 填入会话：
  - `session.collected["phone"]`/`["email"]` 若为空 → 用档案预填 → **contact 卡直接认为已提供**（不重问）。
  - 将 `name/vehicle` 拼进上下文（供个人化回答）。
- **关联记录**：`lead_record` / `rescue_ticket` 写 `user_id`（DB 加列）。

**c) `state.Session`**：加 `user_id` 字段；`anonymous`（无 user_id 时 True）。

**d) `db`**：`leads / escalations / rescue_tickets` 加 `user_id` 列。

## 4. 行为差异
| 场景 | 匿名 | 非匿名(user_id) |
|---|---|---|
| 联系方式 | 每次收集 → 新 lead | **档案已有 → 不重问**，直接用/更新 |
| 称呼 | "您好" | "您好，{name}" |
| 车型 | 无 | 已知 vehicle → 对应售后/保养 |
| 记录 | 无归属 | 挂 user_id |
| 会话历史 | 仅本会话 | 可跨设备找回 |
| 合规 | 每次 consent | 已授权可复用(仍 PDPA/Privacy Act) |

## 5. 数据流（一次请求）
```
前端 send() 加 userId
  → POST /api/v1/chat {sessionId,message,market,langHint,userId}
  → server: user_id = body.userId → pipeline.chat(user_id)
  → 会话.user_id = user_id; collected 预填(电话/邮箱/车型)
  → 知识上下文(含个人化) + 一次 LLM respond
  → 卡片(contact 步因已提供 → 直接达成留资) / 引导
  → 落库(lead 挂 user_id) + 响应(含 userId 相关状态)
```

## 6. 需要宿主配合
- 在聊天窗页面注入 `window.__DSH_USER__`（至少 `{id}`；能带 `name/phone/email/vehicle` 更好）。
- 或后端按 `user_id` 自行查档案（若宿主已有用户 API/DB）。

## 7. 边界
- **未登录/无 user_id** → 纯匿名，现有逻辑不变。
- **user_id 存在但档案缺失** → 仍不索要联系方式可能不成立 → 可回退"询问再留"（由档案是否提供 phone/email 决定）。
- **合规**：非匿名也要在**确实收集/更新联系方式**时给 consent；复用授权需确认已同意且未过期。
