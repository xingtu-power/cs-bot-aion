# 智能客服系统 产品文档 v0.4(as-built)

> AION 埃安海外市场(泰国 / 澳洲)智能客服 · **RAG 对话机器人**
> 版本:v0.4 · 日期:2026-09-03 · 状态:**现行实现(as-built)** · 由 v0.3 设计演进而来

> **说明**:v0.3 是"设计稿";v0.4 是**真正实现的系统**。本文描述当前运行的实际架构、模块、指标与关键决策。各阶段执行报告见文末。

---

## 1. 定位

面向泰国、澳洲(及更多)市场的 AION 新能源车主与潜客的`意图驱动 + RAG 生成`智能客服。

- **P0 问题解决** / **P1 留资+预约转化** / **P2 资料下载+咨询答疑**。
- 核心:**RAG**——从本地化知识库检索 → LLM 生成回答;**意图路由 + 状态机**驱动转化流程。

## 2. 一次对话的处理流程(现行)

```
用户消息 → [POST /api/v1/chat]
   │
① 会话恢复      匿名 session id → 加载状态(断点续聊)
② 语言检测      脚本(泰/中)+ 关键词(英/马/印/西)+ LLM 兜底 → lang
③ 市场路由      开放市场:用户指定→粘性;否则按语言推断(es→ES…);消息里说市场(如"印度市场")→ IN
④ 内容安全      prompt injection 检测 → 命中则拒绝并拉回主题
⑤ ★一次 LLM `respond` 调用(核心,约 1-2s)
     输入: 消息 + 向量语义上下文 + 当前步骤 state_desc + 对话语言 + 最近对话历史
     输出: {intent, confidence, question, response}(4 元组)
       · 语言:按用户消息语言回复;无语言则沿用对话语言(不写死语言码)
       · 用最近对话理解追问,不重复说过的
     --- 注:语言检测仅对内置词表未覆盖的语言才走 LLM 兜底(绝大多数语言零额外 LLM 调用),
         故"每轮一次 LLM"适用于响应+意图生成主路径。
⑥ 意图规整 + 紧急安全预检 + 分流
     normalize_intent(LLM 自造标签→标准 6 类)
     emergency_hit 强信号 → 强制 emergency(覆盖 LLM 漏判)
     关键词纠正:关键词给出置信业务意图且与 LLM 不一致 → 以关键词为准
     分流:
       · 已锁定意图,用户跳到另一业务意图 → switch_intent 切换(不死抠某步)
       · 业务意图 → 锁定;other → 回应介绍+引导
       · 低置信且非业务意图 → 澄清(A/B/C/D 选项),超 CLARIFY_MAX_ROUNDS 转人工
⑦ 卡片状态机(run_card)
     校验输入(手机/邮箱/关注点/确认/救援同意,Python 确定性校验)
     推进步骤 / 达成目标 / 合规留资 / 救援工单落库
⑧ 降级/兜底
     respond 失败 → 重试 3 次(退避)→ dsh headless → 兜底简短话术(永不空回复)
⑨ 输出过滤(内容安全禁区)+ 落库(SQLite)+ 持久化 + 返回
```

## 3. 关键架构决策

### 3.1 RAG 回答生成(每轮一次 LLM)
- **检索 + 生成合一**:一次 `respond` 用"向量语义上下文 + 步骤 + 对话历史"生成回复文本。
- **语言跟随用户**:prompt 只说"按用户语言回复,无语言沿对话语言",**不写死语言码**→ 任意语言都能说,不靠模板。
- **单次调用**:intent+question+response 一次 LLM 输出;不再"意图 + 回答"两次调用。

### 3.2 知识检索:结构化 + 向量混合
| 数据 | 检索 |
|---|---|
| 文档(手册/救援/快速指南) | **向量语义**(e5-large) |
| FAQ(4 语言) | **向量语义** |
| 配置规格 | **结构化精确** |
| 经销商(经纬度) | **距离排序** |

- 向量索引**离线构建**(`tools/embed_index.py`,THA=274 / AU=390 chunks × 1024);模型**共享单例**、启动预热;在线单条查询快。
- `kb.context` = 向量 top-k + 结构化规格;索引缺失自动回退关键词。

### 3.3 多语言(中/英/泰/西为原生,其它 LLM 兜底)
- **卡片状态机**不再有手写 4 语言模板;回复文本全由 LLM 生成。
- **FAQ 4 语言烘焙**(`tools/build_faq_localized.py`,AU/THA 各 4 语言);规格多语言标签。
- 语言检测:脚本(泰/中)+ 关键词(英/马/印/西子串匹配)+ LLM 兜底(其它语言)。
- **严格跟随当前消息语言**:提示词规定"当前消息语言**始终优先**",即使整段会话/知识为另一语言,只要用户切换语言就按新语言回复;中性消息(如 yes/thanks/电话号码)才回退到对话语言。`session.language` 随最近一次检测更新,用于中性消息兜底。

### 3.4 意图(准确率优先)
- LLM `respond` 判意图 → `normalize_intent`(子串匹配规整)→ `emergency_hit` 安全预检 → 关键词纠正。
- **多语言意图准确率 ≈98%**(评测集 57 条:中/英/泰/西 × 6 意图;`tools/eval_intent_ml.py`)。

### 3.5 卡片状态机(引导卡)
- 不再手写话术模板;每卡只有 `state_desc`(英文情景描述给 LLM)+ `expect`(校验模式)+ 步骤/目标。
- 校验/收集/达成/合规均为 Python 确定性逻辑;回复文本由 LLM 生成。

### 3.6 延迟(已优化)
- 直接调用 DeepSeek OpenAI 兼容 API(不再每消息 boot agent)。
- e5 模型共享单例 + 启动预热(消除 30s 首载)。
- **单轮约 1-2s**。

### 3.7 可观测性
- **debug 状态默认开**(`CSAPP_DEBUG=0` 关):每轮记录 `llm_respond_input/raw/ok(或 parse_fail)`、`llm_api_retry/failed`、`turn` 摘要到 `data/debug.jsonl`;查询 `GET /api/v1/debug`。

### 3.8 回复质量护栏(参考售前通用框架,在单次 LLM 调用内完成)
- **长度硬限**:提示词要求回复 ≤`REPLY_DAILY_MAX`(默认 220),绝对上限 `REPLY_MAX_CHARS`(默认 300);超限在**句读边界截断**。
- **禁 AI 感/机器感**:提示词禁止暴露 AI/机器人身份、禁止"according to the search results / the system shows / 根据查询结果 / 系统显示"等表达;输出侧 `compliance.strip_ai_phrases` 兜底剥离。
- **话术去重**:提示词禁止复读;输出侧 `_dedup_lead` 若新回复首句与近 3 轮 bot 首句相似度 ≥`REPEAT_SIM_THRESHOLD`(0.8)则裁掉该首句。
- 全部在管道 `_polish_reply` 内完成,**不额外调用 LLM**,保持每轮一次调用。

### 3.9 槽位收集护栏(参考售前通用框架)
- 需用户输入的槽位(`contact`/`concern`/`confirm`/`consent_rescue`)若一直未满足,按 `SLOT_MAX_ASK`(默认 3)累计;超限**转人工**(`session.step_asks` 计数,键 `intent:step`,跨轮持久化)。
- 已收集且有效的槽位不重问(如之前已留电话,换意图进入 contact 卡时直接触发 goal)。

## 4. 模块清单

| 文件 | 职责 |
|---|---|
| `pipeline.py` | 端到端编排(会话→语言→市场→respond→意图规整→状态机→**回复质量 `_polish_reply`**→落库) |
| `llm.py` | LLM 抽象(`respond`/`classify`/`detects_language`/`localize`;直接 API + 重试 + headless 兜底;读凭据 key) |
| `cards.py` | 卡片状态机(每卡 state_desc + expect + 校验 + 推进/达成/合规留资/救援 + **槽位追问上限转人工**) |
| `kb.py` | 知识库(结构化规格/FAQ/经销商 + `VectorRetriever` 向量检索 + `context`) |
| `intent.py` | 意图(`normalize_intent`/`emergency_hit`/关键词/`recognize`) |
| `langdetect.py` | 语言检测(脚本+关键词+LLM 兜底) |
| `market.py` | 市场路由(开放 + 语言推断 + 手动粘性 + 消息提取市场) |
| `state.py` | 会话状态机(匿名 id + 持久化 + 断点续聊;排除瞬态 `_` 属性) |
| `compliance.py` | 合规(同意条款 4 语言/留资记录/PII 去重)+ 内容安全(禁区/输出过滤/injection)+ 回复质量(禁AI感 `strip_ai_phrases` / 截断 `truncate_reply`) |
| `db.py` | 自建库(SQLite:leads/escalations/rescue_tickets,幂等) |
| `emotion.py` | 情绪评分 |
| `server.py` | 零依赖 HTTP:`/`(前端页)/`api/v1/{chat,lead,escalate}`/`stats`/`leads`/`debug`;启动时 warmup 预热向量 |
| `api.py` / `cli.py` | FastAPI 版 / 交互 CLI |
| `static/index.html` | 原生单页聊天窗(免登录,示例按钮) |
| `tools/` | ETL / 评测 / 向量索引 / FAQ 烘焙 / 泰文 CMap 还原 |

## 5. 当前指标

| 指标 | 值 |
|---|---|
| 多语言意图准确率 | **100%**(57 条评测集;校准报告 98%,修复 battery-is-dead 后实测 100%) |
| 单轮延迟 | **~1-2s**(直接 API + 共享模型) |
| 泰文向量检索 recall@5 | e5-large 0.186 vs MiniLM 0.129(+44%) |
| 语言 | 中/英/泰/西 原生;其它 LLM 兜底 |
| 市场 | 开放(任意码);按语言推断 / 手动粘性 / 消息提取 |

## 6. 与 v0.3 的演进(v0.4 相比 v0.3 的关键变化)

1. **从"模板驱动引导卡"→"LLM 生成回复 + 卡片状态机"**:删手写 4 语言模板。
2. **从"关键词检索"→"向量语义检索"**(e5-large)。
3. **从"意图+回答两次 LLM"→"一次 `respond` 调用"**。
4. **语言不写死**:按用户语言、无语言沿对话语言(不再靠模板维护)。
5. **加入**:紧急安全预检、关键词纠正、normalize_intent、debug 状态、直接 API+重试、共享模型+预热、对话历史。
6. **市场开放化** + 语言→市场推断 + 消息里提取市场。

## 7. 执行报告(各阶段)
- `Phase0_REPORT.md`(知识地基)、`Phase1_REPORT.md`、`PHASE1_LLM_W23_REPORT.md`
- `THAI_RESTORE_REPORT.md`(CMap 还原)、`REFACTOR_VECTOR_REPORT.md`(架构重构+向量)
- `INTENT_CALIBRATION_REPORT.md`(意图校准+评测)

## 8. 建议的生产化点
- LLM + 向量在 GPU(进一步降延迟/提质);在线检索 CPU 亦可。
- 索引版本管理(知识更新需重建 `tools/embed_index.py`)。
- 各市场知识库扩充(非 AU/THA 回退 AU)。
- 回答质量黄金评测(不只意图)。
- 前端:调试开关 / 引用展示 / 卡片化。
