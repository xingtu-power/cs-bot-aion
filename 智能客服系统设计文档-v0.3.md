# 智能客服系统设计文档 v0.3

> AION 埃安海外市场(泰国 / 澳洲)智能客服 · 知识咨询型 + 转化导向
> 版本:v0.3 · 日期:2026-09-01 · 状态:已评审(在 v0.2 基础上补齐合规/内容安全/EO 运营/评测与降级)

---

## 0. v0.2 → v0.3 变更摘要

| 章节 | 变更 |
|---|---|
| §1.4 | 新增渠道策略决策(Web 首发 / Line、WhatsApp 适配层) |
| §2.2 | 状态机补充:情绪分、主/次意图、意图切换规则 |
| §5.5 | 新增知识库 ETL 管道与版本管理、泰文 OCR 风险 |
| §6.5 | 新增降级兜底、限流防护、可观测性与 SLO |
| **§7(新)** | **数据合规与安全**(数据分级、保留/删除、PII 保护、审计) |
| **§8(新)** | **AI 内容安全**(禁区清单、免责声明、输出过滤、prompt injection 防御) |
| §9 | 路线图每 Phase 增加退出标准(DoD);泰语冷启动语料 |
| §10 | 评估指标补充黄金评测集、影子/灰度、成本与 SLO |
| §11 | 决策记录新增 6-9(渠道、情绪、回流、DoD 等) |

---

## 1. 项目定位

### 1.1 一句话定位

面向泰国、澳洲市场的 AION 新能源车主与潜客的**意图驱动型智能客服**——不是通用问答机器人,而是按"意图场景 × 引导策略 × 目标达成"组织的转化漏斗。

### 1.2 核心目标(按优先级)

| 优先级 | 目标 | 说明 |
|---|---|---|
| P0 | 问题解决 | 售后/救援/使用类意图必须把用户问题解决掉,这是信任基础 |
| P1 | 线索获取(留资) | 产品/购车类意图,引导留下联系方式 |
| P1 | 预约转化 | 试驾/到店预约 |
| P2 | 资料下载 | 低门槛钩子(配置表、手册) |
| P2 | 咨询答疑 | 高效、准确、带引用地回答 |

### 1.3 语言与市场:两个独立维度(架构级决策)

> **已决策(2026-09-01)**:语言跟随用户,市场决定知识。二者解耦。

| 维度 | 规则 | 决定什么 |
|---|---|---|
| **语言维度** | 自动检测用户输入语言 → 用**用户的语言**回复 | 话术语言、展示层 |
| **市场维度** | 位置/时区/语言提示 + 可手动切换 | 检索哪个知识库(配置表/经销商/手册) |

真实场景示例:
- 泰国用户用英语问澳洲车型 → 语言=en,市场=AU
- 旅居澳洲的泰裔用泰语问泰国经销商 → 语言=th,市场=THA

**内部信息流原则**:状态、留资、转人工摘要等内部流转数据统一使用**结构化存储语言(默认英文)**,仅展示层做语言适配,避免内部数据被多语言污染。

市场知识素材(仍按市场隔离):

| 维度 | 泰国 | 澳洲 |
|---|---|---|
| 配置表 | 泰文配置表 | aionut_spec_0811 |
| 经销商网络 | THA dealer(含经纬度) | AUdealer(含州/城市/店铺类型) |
| 手册 | 车主手册/紧急救援/快速指南(泰文) | Owners/Rescue/Quick start(英文) |

> **设计原则**:同一套意图框架,知识库按市场隔离;意图识别与语言无关;检索按市场路由;回复按用户语言生成。

### 1.4 渠道策略(新增决策)

> **已决策(2026-09-01)**:首期以 **Web 聊天窗** 端到端跑通;**Line(泰国)/ WhatsApp(澳洲)以后端适配层快速接入**,不为其单独重做逻辑,复用同一会话状态机与引导卡引擎。

| 渠道 | 定位 | 说明 |
|---|---|---|
| Web 聊天窗 | 首期主验证载体 | 免登录、匿名会话、浏览器定位能力最全 |
| Line(泰国) | 二期主渠道候选 | 泰国民众主流 IM;复用后端,做 channel 适配 |
| WhatsApp(澳洲) | 二期主渠道候选 | 澳洲主流 IM;同上 |

> **渠道抽象原则**:所有渠道共用同一 `POST /api/v1/chat` 语义与状态机;渠道差异只体现在**入参(channel,user id)与出参(消息格式/按钮能力)**。后端状态与任务不与渠道绑定。

---

## 2. 整体流程状态机

### 2.1 主流程

```
用户进入(Web 聊天窗,免登录)
     │
     ▼
┌──────────────────────────────────────────────┐
│ ① 会话初始化                                   │
│    · 语言检测:自动识别用户输入语言,全程跟随        │
│    · 市场判定:位置/时区/语言提示,可手动切换        │
│    · 加载历史会话状态(匿名会话 ID,断点续聊)       │
└──────────────────────────────────────────────┘
     │
     ▼
┌──────────────────────────────────────────────┐
│ ② 意图识别(语言无关)                            │
│    · 二段式:关键词规则快筛 → LLM 语义确认        │
│    · 输出:意图标签 + 置信度 + 关键实体 + 情绪分    │
└──────────────────────────────────────────────┘
     │
     ▼
③ 路由决策
   ├─ 置信度 < 阈值 ──────────→ 澄清引导(给选项列表,≤2 轮,超限转人工)
   ├─ 闲聊/无关 ─────────────→ 简短回应 + 拉回主题,超限转人工
   ├─ 紧急意图(救援/故障) ────→ 直接进入,无视其他流程,可触发救援工单
   ├─ 主/次意图混合 ─────────→ 确认主意图进入,次意图挂起,主意图完成后可继续
   └─ 命中业务意图 ───────────→ 进入对应意图场景
     │
     ▼
┌──────────────────────────────────────────────┐
│ ④ 意图场景执行(引导卡驱动)                      │
│    读当前状态 → 按引导卡步骤输出话术 → 更新状态   │
└──────────────────────────────────────────────┘
     │
     ├── 达成目标 ────────────→ ⑤ 结束话术 + 满意度反馈
     │                              │
     └── 未达成 ────────────→ 继续引导 / 异议处理 / 降级选项
                                  │
                                  └── 超过轮数上限 / 用户要求 / 高情绪(≥4)
                                          │
                                          ▼
                                    ⑥ 转人工(结构化事件,人工看记录回访)
```

### 2.2 状态机定义

每个会话维护一个持久化状态对象:

```json
{
  "sessionId": "sess_xxx",
  "market": "THA" | "AU" | null,
  "marketSource": "auto" | "manual",
  "language": "th" | "en" | "zh" | ...,
  "intent": "product-inquiry" | "dealer-lookup" | "usage-guide" | "emergency" | "after-sales" | "other" | null,
  "intentMain": "product-inquiry" | null,        // 新增:主意图(多意图时)
  "intentSecondary": ["dealer-lookup", ...] | [], // 新增:挂起的次意图
  "confidence": 0.0,
  "emotionScore": 0,                              // 新增:1-5 情绪分(见 §2.3)
  "stepIndex": 0,
  "collected": { "phone": null, "email": null, "model": null, "concern": null },
  "clarifyRounds": 0,
  "totalRounds": 0,
  "escalated": false,
  "resolved": false,
  "targetReached": false,
  "rescueTicketId": null,
  "anonymous": true,
  "leadId": null
}
```

状态变化规则:
- 每轮对话结束必须持久化,支持断点续聊(匿名会话 ID 维系)
- 意图一旦确认,在本会话内除非用户明确切换,否则不重新识别
- 紧急意图一旦触发,锁定场景直至解决或转人工(**可插队抢占主意图**,见 §2.4)
- 市场可在用户明确要求时手动切换;语言在检测到切换信号时跟随(如用户换语言提问)
- 多意图时保存 `intentMain` + 挂起 `intentSecondary`,主意图完成后再逐个处理

### 2.3 情绪识别规则(新增,量化"高情绪转人工")

| 维度 | 信号 | 加权 |
|---|---|---|
| 关键词/情绪词 | 愤怒、投诉、威胁、报警、损失、事故、timewasting、angry | +1(每类)|
| 语气 | 全大写、连续重复(如 "!!!")、极端标点 | +1 |
| 内容风险 | 人身安全、财产损失、事故、车辆无法移动 | +2(直接触发紧急加权)|
| 紧急意图关联 | `emergency` 场景下情绪分 **加权 ×1.5** | — |

> **升级阈值**:`emotionScore ≥ 4` 或连续两轮 `≥ 3` → 转人工;`emergency` 场景下 `≥ 3` 即触发。情绪分随每轮更新并落库,作为转人工摘要的一部分。

### 2.4 多意图与意图切换(补充状态机规则)

- **多意图混合**(如"UT 续航多少 + 最近经销商在哪"):LLM 判定主/次意图 → 主意图进入场景,次意图挂起;主意图达成后若用户仍有意向,继续处理次意图。
- **会话中途切换到 emergency**:紧急意图可随时**插队抢占**当前场景;切换到 emergency 后,原意图的 `collected` / `stepIndex` **保留在状态中**,紧急解决后引导用户回到原场景(或礼貌结束)。
- **用户主动切意图**(明确"别聊这个,我要问xxx"):重置 `stepIndex`、`collected` 中与该意图无关字段,重新进入新意图。

---

## 3. 意图清单(完整版)

### 3.1 意图总表

| ID | 意图 | 目标 | 紧急度 | 知识来源 | 达成标志 |
|---|---|---|---|---|---|
| `product-inquiry` | 产品咨询 | 留资/预约试驾 | 正常 | 配置表 | 有效联系方式 |
| `dealer-lookup` | 经销商查询 | 留资/到店预约 | 正常 | 经销商表 | 有效联系方式 |
| `usage-guide` | 使用指导 | 问题解决 | 正常 | 快速指南/车主手册 | 用户确认解决 |
| `emergency` | 故障/救援求助 | 解决+紧急升级 | **最高** | 紧急救援手册 | 用户确认或已转接 |
| `after-sales` | 售后/保养咨询 | 解决+满意度 | 正常 | 手册/质保 | 用户确认解决 |
| `other` | 闲聊/无关 | 最小打扰 | 正常 | — | 拉回或转人工 |

### 3.2 意图识别信号(语言无关,按市场语言扩充)

| 意图 | 关键词快筛(示例,按市场语言扩充) | 语义确认要点 |
|---|---|---|
| product-inquiry | 配置、续航、价格、车型、版本、spec、range、price | 咨询对象是产品本身 |
| dealer-lookup | 经销商、哪里买、营业、地址、dealer、where to buy | 找线下/购买渠道 |
| usage-guide | 怎么充电、怎么启动、怎么用、how to、充电口 | 功能使用而非故障 |
| emergency | 抛锚、无法启动、报警、救援、事故、breakdown、rescue | 车辆无法正常使用 |
| after-sales | 保养、质保、配件、维修、service、warranty | 售后类问题 |
| other | 问候、闲聊、无关话题 | 无业务意图 |

> 关键词表按语言分桶维护(中/英/泰),LLM 语义确认阶段不依赖关键词,直接对原文理解。

### 3.3 澄清与置信度规则

- 二段式:`关键词命中 && LLM 语义确认` → 高置信度直接进入
- 仅关键词命中 → 低置信度,用选项列表澄清(选项用用户当前语言呈现):"您是想了解 **A. 车辆配置价格 B. 经销商位置 C. 使用问题 D. 其他**?"
- 澄清 ≤ 2 轮;仍无法确定 → 转人工

---

## 4. 意图引导卡(核心契约)

> 引导卡是产品/运营与 agent 之间的执行契约:agent 按卡内步骤推进,不得自由发挥偏离目标。所有话术按用户语言生成。

### 4.1 引导卡通用模板

```
意图:<id> <名称>
├─ 进入判定:<触发条件>
├─ 目标:<达成目标>
├─ 引导步骤:
│   <步骤序号> <话术要点> <信息收集点>
├─ 异议处理:<常见异议 → 应对>
├─ 达成判定:<客观校验规则>
└─ 失败路径:<降级选项 / 转人工条件>
```

### 4.2 引导卡:产品咨询(product-inquiry)

```
意图:product-inquiry 产品咨询
├─ 进入判定:咨询车型/配置/续航/价格,无售后信号
├─ 目标:留资(手机/邮箱)→ 销售跟进
├─ 引导步骤:
│   ① 回应关切:引用配置表真实数据回答,标注车型(如 AION UT)
│   ② 挖掘需求:"您最关注续航、空间还是价格?"→ 记录 concern
│   ③ 价值锚点:"当地经销商可安排试驾/提供详细报价"
│   ④ 留资钩子:"留下联系方式,我们帮您对接当地经销商,并提供定制报价"
├─ 合规前置:留资话术前必须展示同意条款(个人信息收集告知,PDPA/Privacy Act 合规)
├─ 异议处理:
│   嫌贵 → 强调质保/服务网络/用车成本
│   比价 → 差异化(配置、售后网络覆盖)
│   怕打扰 → 承诺"仅销售对接一次,不外泄"
├─ 达成判定:手机号格式校验(泰 09/06/08 开头 10 位 / 澳 04 开头 10 位)或邮箱含 @
└─ 失败路径:3 轮未留资 → 降级给经销商信息;用户拒绝 → 尊重,给资料下载
```

### 4.3 引导卡:经销商查询(dealer-lookup)

```
意图:dealer-lookup 经销商查询
├─ 进入判定:找店/购买渠道/营业时间/地址
├─ 目标:留资(到店预约)
├─ 引导步骤:
│   ① 定位:获取用户位置(城市/州/经纬度,可复用浏览器定位)
│   ② 匹配:按距离返回最近 1-3 家(名称、地址、类型、营业时间、电话)
│   ③ 预约钩子:"需要我帮您预约到店看车吗?留个联系方式即可"
├─ 异议处理:不在服务区 → 推荐线上资料/留资跟进
├─ 达成判定:有效联系方式 + 预约意向确认
└─ 失败路径:2 轮未预约 → 直接给经销商信息(低门槛达成),不强迫
```

### 4.4 引导卡:使用指导(usage-guide)

```
意图:usage-guide 使用指导
├─ 进入判定:功能使用问题(充电/启动/设置)
├─ 目标:用户确认问题解决
├─ 引导步骤:
│   ① 明确车型与具体功能(如充电口位置、快慢充)
│   ② 按手册分步讲解,引用手册章节(附页码/章节号)
│   ③ 确认:"按这个步骤操作是否可以了?"
├─ 达成判定:用户明确确认解决
└─ 失败路径:步骤后仍无法解决 → 转人工(可能是故障而非使用问题)
```

### 4.5 引导卡:故障/救援求助(emergency)· 最高优先级

```
意图:emergency 故障/救援求助
├─ 进入判定:抛锚/无法启动/报警灯/事故/救援 + 情绪信号
├─ 目标:快速定位 + 紧急处理,不追求留资
├─ 引导步骤:
│   ① 安抚 + 前置安全确认:"您和乘客是否安全?车辆是否在安全位置?"
│   ② 分步排查(仅限可远程判断项):仪表盘报警含义、重启尝试
│   ③ 无法解决 → 立即提供:最近经销商/救援热线(按位置匹配)+ 紧急救援手册对应章节
│   ④ 触发救援工单(见 §4.8 触发契约),用户同意后创建
├─ 达成判定:用户确认解决 或 已转接救援
├─ 免责声明(强制):本场景须前置官方救援热线 + "AI 不能替代专业救援/官方热线" 声明
└─ 特殊规则:本意图优先于一切流程;不设留资目标;可跳过其他引导步骤直接给救援信息
```

### 4.6 引导卡:售后/保养咨询(after-sales)

```
意图:after-sales 售后/保养咨询
├─ 进入判定:保养/质保/配件/维修问题
├─ 目标:解决 + 满意度
├─ 引导步骤:
│   ① 明确问题类型(保养周期/质保范围/配件购买)
│   ② 按手册/质保政策回答,引用来源
│   ③ 需要到店 → 提供经销商 + 可预约
├─ 达成判定:用户确认解决 或 完成到店预约
└─ 失败路径:政策外问题 → 转人工(售后专员)
```

### 4.7 引导卡:其他(other)

```
意图:other 闲聊/无关
├─ 进入判定:无业务意图
├─ 目标:最小打扰,拉回业务
├─ 引导:简短友好回应 → 一次拉回:"关于 AION 车辆,我可以帮您解答配置、经销商或使用问题"
└─ 失败路径:拉回后仍闲聊 ≥2 轮 → 转人工或礼貌结束
```

### 4.8 救援联动触发契约(已决策:接口先行,备用启用)

> **已决策(2026-09-01)**:现阶段仅聊天通道;电话/短信联动**接口先行**,按需启用。通道抽象化,预留 Line(泰国)/WhatsApp(澳洲)。

**触发条件**(满足任一):
1. 用户明确要求电话联系/救援
2. 关键安全动作确认后无法自行解决(无法启动、事故、人员风险)
3. 对话中断且处于紧急场景(用户失联)

**数据最小集**(创建救援工单所需):
```json
{
  "ticketId": "rsq_xxx",
  "sessionId": "sess_xxx",
  "channel": "chat",                 // 当前通道;未来 sms|phone|line|whatsapp
  "market": "THA" | "AU",
  "location": { "lat": 13.75, "lng": 100.5 },
  "vehicle": { "model": "AION UT", "plate": null },
  "issueSummary": "车辆无法启动,仪表盘显示...",   // 内部统一存储语言
  "userConsent": true,
  "createdAt": "2026-09-01T00:00:00Z"
}
```

**联动通道(备用实现)**:`POST /api/v1/rescue/ticket`(创建工单)→ 按需触发 `sms`(发救援热线短信)/ `phone`(回拨)/ 未来 `line|whatsapp`。各通道是独立异步任务,不阻塞对话流。

---

## 5. 知识库设计

### 5.1 素材盘点(现状)

| 市场 | 类型 | 文件 | 结构化程度 |
|---|---|---|---|
| 泰国 | 售后手册 | 车主手册 / 紧急救援 / 快速指南(PDF,泰文) | 非结构化 |
| 泰国 | 产品配置表 | 泰国UT配置表(泰文 xlsx) | 半结构化 |
| 泰国 | 经销商信息 | THA dealer.xlsx(network/customer code/经纬度/地址/电话) | 结构化 |
| 澳洲 | 用户手册 | Owners / Emergency Rescue / Quick start(PDF,英文) | 非结构化 |
| 澳洲 | 产品配置表 | aionut_spec_0811.xlsx | 半结构化 |
| 澳洲 | 经销商信息 | AUdealer.xlsx(州/城市/店名/类型/经纬度) | 结构化 |

### 5.2 知识库分层

```
知识库
├─ 市场层(隔离):THA / AU 各自独立的检索空间
│   ├─ 文档知识(PDF 切分 → 向量库)
│   │   元数据:market, doc_type(owner/rescue/quickstart), page, chapter, version, effective_date
│   ├─ 结构化知识(配置表/经销商表 → 记录型数据)
│   │   配置:model, variant, spec_item, spec_value, unit
│   │   经销商:market, name, address, city, state, type, phone, lat, lng, hours
│   └─ FAQ 知识(高频问答对,运营维护)
├─ 规则层(全局):引导卡、意图规则、合规约束、内容安全禁区(共享)
└─ 会话层(动态):用户上下文、已收集信息(不跨会话共享,隐私隔离)
```

### 5.3 多语言检索策略(已决策:语言跟随用户)

> **已决策(2026-09-01)**:用户用什么语言,就用什么语言沟通;内部信息流(状态/留资/工单)统一存储语言(默认英文)。

| 策略 | 说明 | 优先级 |
|---|---|---|
| **原语检索** | 用户语言直接嵌入检索(英文文档用英文查,泰文文档用泰文查) | 首选 |
| **翻译检索兜底** | 原语检索质量不足时,翻译成文档语言再检索 | 兜底 |
| **内部语言** | 所有落库数据(lead/ticket/summary)统一英文 | 恒定 |

**Phase 0 验证项**:泰语场景下"泰语嵌入直接检索" vs "翻译成英文再检索"的质量对比,决定泰语检索默认策略。

### 5.4 关键检索场景

| 场景 | 检索策略 |
|---|---|
| "UT 续航多少"(任意语言) | 语言无关的意图识别 → 结构化配置表精确查询(市场路由) |
| "怎么充电" | 文档向量检索(市场手册,语言=用户语言或翻译兜底) |
| "最近经销商" | 结构化查询 + 距离排序(经纬度 Haversine) |
| 混合问题 | 配置表优先,文档兜底,LLM 汇总并标注引用 |

### 5.5 知识库 ETL 管道与版本管理(新增)

**ETL 管道(PDF→向量 | xlsx→结构化表)**:

```
原始素材(市场隔离目录)
   │
   ├─ PDF(售后手册)──管道 A──→ 文档知识
   │   ① 前置检查:是否为文本型 PDF?若为扫描件 → 标记需 OCR(泰文 OCR 精度是硬风险,Phase 0 必须先验证)
   │   ② 切分(按章节/页码,保留 page/chapter 元数据)
   │   ③ 嵌入 + 入库(需带 market / doc_type / version / effective_date)
   │
   └─ xlsx(配置表 / 经销商表)──管道 B──→ 结构化表
       ① 清洗(去重、规范化、字段映射)
       ② 校验(必填字段、经纬度范围、格式:手机号/营业时间)
       ③ 类型化(配置:model/variant/spec_item/spec_value/unit;经销商:含 lat/lng)
       ④ 入库(带 version / effective_date)
```

> **泰文 OCR 风险**(P0):泰文 PDF 若为扫描件,OCR 准确率可能显著劣化,影响检索质量。**Phase 0 前置检查**:确认是否为文本型 PDF;若是扫描件,先做小样本 OCR 精度验证再决定是否投入全量。

**知识版本管理**:
- 每份知识素材带 `version`(如 `spec_0811`)与 `effective_date`,并保留历史版本。
- 配置表/手册更新时,**增量 diff** → 更新结构化记录 / 重嵌入变更章节;禁止新旧混用检索结果。
- 检索时按 `market + effective_date ≤ 当前` 过滤,确保只用"当前生效版本"。
- 重要变更(如价格、配置变化)需记录变更日志,便于审计与回滚。

**FAQ 运营流程**:
- 新增:运营提交 → 市场负责人审核 → 上线 → 沉淀到黄金评测集。
- 退出:命中率低 / 重复提问 → 标记下架,并从评测集移除。
- 双市场**各自独立维护** FAQ,语言与内容分市场管理。

---

## 6. 技术架构与接口定义

### 6.1 组件架构

```
┌─────────────────────────────┐
│ Web 客服前端(聊天窗,免登录)    │
│ · 匿名会话 ID · 语言自适应     │
│ · 参数化卡片(经销商/电话/预约卡)│
└──────────────┬──────────────┘
               │ WebSocket / HTTP
┌──────────────▼──────────────┐
│ 客服后端服务                  │
│ · 会话管理(状态机持久化)        │
│ · 语言检测 + 市场路由          │
│ · 意图路由(规则+LLM,含情绪识别)│
│ · 引导卡引擎(状态→话术)        │
│ · 留资/工单落库(自建库)        │
│ · 转人工事件(人工看记录回访)    │
│ · 救援联动桥接(备用)           │
│ · 内容安全过滤 · 降级兜底       │
└──────┬───────────────┬──────┘
       │               │
┌──────▼──────┐  ┌─────▼────────┐
│ 知识检索服务  │  │ Agent 生成层   │
│ · 向量库     │  │ · 多语言生成   │
│ · 结构化库   │  │ · 引用校验    │
│ · FAQ       │  │ · 合规过滤    │
│ (含版本管理) │  │ · 内容安全过滤 │
└─────────────┘  └──────────────┘
```

### 6.2 核心接口

```yaml
# 发送消息(用户 → 客服,免登录,匿名会话)
POST /api/v1/chat
{
  "sessionId": "sess_xxx",          # 匿名会话 ID,续聊时传;新会话留空
  "message": "UT 的续航是多少?",     # 任意语言
  "location": { "lat": 13.75, "lng": 100.5 }   # 可选,经销商查询用
}
→ 200 {
  "sessionId": "sess_xxx",
  "language": "zh",                 # 检测并跟随用户语言
  "market": "THA",                  # 市场路由结果
  "marketSource": "auto",
  "intent": "product-inquiry",
  "reply": "AION UT ...",            # 按用户语言生成
  "citations": [{ "doc": "配置表", "page": null, "version": "spec_0811" }],
  "state": { "stepIndex": 2, "collected": { "concern": "续航" } },
  "emotionScore": 2,                 # 返回情绪分供前端/监控
  "cards": ["dealer-list", "call-to-action"],  # 新增:参数化卡片指令
  "targetReached": false,
  "escalate": false
}

# 提交留资(自建库)
POST /api/v1/lead
{ "sessionId": "sess_xxx", "intent": "product-inquiry",
  "consent": true,                  # 同意条款(合规必需)
  "consentVersion": "v1",           # 同意条款版本(审计)
  "lead": { "name": "...", "phone": "099...", "email": "..." } }
→ 200 { "ok": true, "leadId": "lead_xxx" }

# 创建救援工单(联动接口,备用)
POST /api/v1/rescue/ticket
{ "sessionId": "sess_xxx", "market": "THA",
  "location": { "lat": 13.75, "lng": 100.5 },
  "vehicle": { "model": "AION UT" },
  "issueSummary": "车辆无法启动...",   # 内部统一存储语言
  "userConsent": true,
  "channels": ["sms", "phone"] }     # 通道抽象:chat|sms|phone|line|whatsapp
→ 200 { "ok": true, "ticketId": "rsq_xxx" }

# 转人工(结构化事件,人工看记录回访)
POST /api/v1/escalate
{ "sessionId": "sess_xxx", "reason": "clarify-timeout|emotion-high|usage-unresolved",
  "emotionScore": 4,
  "contextSummary": "用户咨询UT价格,3轮未留资...",
  "timeline": [ ... ], "collected": { ... } }
→ 200 { "ok": true, "ticketId": "tkt_xxx" }

# 满意度反馈
POST /api/v1/feedback
{ "sessionId": "sess_xxx", "rating": 5, "comment": "..." }

# 数据删除(被遗忘权)
POST /api/v1/data/delete
{ "sessionId": "sess_xxx" }        # 删除该会话及关联 PII/日志(合规)
→ 200 { "ok": true }

# 管理端:人工回访状态流转(可选后台)
PUT /api/v1/escalations/{id}
{ "status": "contacted|converted|closed", "handler": "...", "note": "..." }
```

### 6.3 数据落库 Schema(自建库)

**leads(留资线索)**
```sql
CREATE TABLE leads (
  id TEXT PRIMARY KEY,              -- lead_xxx
  session_id TEXT NOT NULL,
  market TEXT NOT NULL,             -- THA | AU
  channel TEXT NOT NULL DEFAULT 'web',
  intent TEXT NOT NULL,
  name TEXT,
  phone TEXT,                       -- 加密存储
  email TEXT,
  consent_at TIMESTAMP NOT NULL,    -- 同意条款时间(合规)
  consent_version TEXT,             -- 同意条款版本(审计)
  status TEXT DEFAULT 'new',        -- new|contacted|converted|closed
  raw_json TEXT,                    -- 原始上下文快照
  dedupe_key TEXT,                  -- 归一化手机号,用于跨会话去重
  created_at TIMESTAMP NOT NULL DEFAULT now(),
  UNIQUE (dedupe_key, market)
);
```

**escalations(转人工事件)**
```sql
CREATE TABLE escalations (
  id TEXT PRIMARY KEY,              -- tkt_xxx
  session_id TEXT NOT NULL,
  reason TEXT NOT NULL,
  emotion_score INT,                -- 触发时的情绪分
  context_summary TEXT NOT NULL,    -- 自动生成,内部统一语言
  timeline_json TEXT,
  collected_json TEXT,
  status TEXT DEFAULT 'open',       -- open|contacted|converted|closed
  handler TEXT,                     -- 人工回访记录人
  created_at TIMESTAMP NOT NULL DEFAULT now()
);
```

**rescue_tickets(救援工单,备用)**
```sql
CREATE TABLE rescue_tickets (
  id TEXT PRIMARY KEY,              -- rsq_xxx
  session_id TEXT NOT NULL,
  market TEXT NOT NULL,
  channel TEXT NOT NULL DEFAULT 'chat',
  lat REAL, lng REAL,
  vehicle_model TEXT, plate TEXT,
  issue_summary TEXT,
  user_consent BOOLEAN NOT NULL,
  status TEXT DEFAULT 'open',
  created_at TIMESTAMP NOT NULL DEFAULT now()
);
```

**knowledge_documents(文档知识索引,新增)**
```sql
CREATE TABLE knowledge_documents (
  id TEXT PRIMARY KEY,
  market TEXT NOT NULL,             -- THA | AU
  doc_type TEXT NOT NULL,           -- owner|rescue|quickstart|faq
  page INT, chapter TEXT,
  source_file TEXT,
  version TEXT,                     -- 如 spec_0811
  effective_date DATE,              -- 生效日期
  content TEXT,
  embedding_id TEXT,
  created_at TIMESTAMP NOT NULL DEFAULT now()
);
```

**data_retention_policy(数据分级与保留,新增)**
| 数据 | 分级 | 默认保留 | 删除依据 |
|---|---|---|---|
| 会话日志 | 高(含上下文) | 180 天 | 用户删除请求 / 到期自动 |
| leads | 高 | 依业务 / 合规 | 用户删除请求;成交后另定 |
| escalations | 高 | 180 天 | 用户删除请求 |
| rescue_tickets | 高 | 依合规 | 用户删除请求 |

### 6.4 与 DSH 资产的结合方式(可选路线)

- 引导卡引擎 → 内嵌为 `customer-support` agent 预设的 persona 指令(引导卡结构化注入)
- 后端通过 headless / 编程式调用创建 agent,`meta.cwd` 指向客服知识库目录
- 会话状态持久化 → 复用 DSH session 机制
- 转人工/救援事件 → 会话上下文摘要直接取自持久化日志

### 6.5 降级兜底、限流防护、可观测性与 SLO(新增)

**降级兜底(服务不可用时的链)**
```
正常:LLM → 引导卡
LLM 不可用 → 规则引擎 + FAQ 精确匹配兜底 → 仍失败 → 转人工
emergency 无 LLM:
  · 必须有离线规则:根据 `market + location` 直接输出官方救援热线(不依赖 LLM)
  · 兜底话术:预先固化多语言模板(安全提示 + 官方热线)
```

**限流与滥用防护**
- 匿名会话 + 留资:按 IP/session 限流(如 ≤ N 次留资/小时),超出触发人机校验(验证码)。
- 单会话轮数上限(防御粘住/刷量):如 ≤ 50 轮强制转人工。
- Prompt injection 防御:见 §8.3。

**可观测性(每轮日志字段)**
- `sessionId, channel, market, language, intent, confidence, emotionScore, targetReached, escalate, latency_ms, degraded(bool), model/version`
- 指标:`p95_latency`、意图识别率、答疑引用缺失率、`degraded 比例`、`escalate 比例`、`lead 转化率`、`满意度`。
- 告警:`意图识别率 24h 环比骤降 >5%`、`degraded 比例 >10%`、`p95 时延 >3s`、`emergency 无兜底命中`。

**性能 SLO(建议)**
- 对话响应 p95 < 3s;紧急场景首响应 < 1.5s。
- 每会话平均 token 成本预算(泰语 LLM 单价假设需监控,给上限)。

---

## 7. 数据合规与安全(新增)

> 面向泰国(PDPA)与澳洲(Privacy Act 1988 + APPs)市场,合规是不可绕过的硬约束。

### 7.1 数据分级与最小化

- 数据分级:会话上下文(高)、留资 PII(高)、工单(中高)、脱敏统计(低)。
- **最小化原则**:仅在需要时采集(留资只在产品/购车类意图;紧急场景不采集非必要 PII);每个接口只返回必要字段。

### 7.2 保留与删除

- **分级保留策略**:见 §6.3 `data_retention_policy`(会话 180 天、工单 180 天等)。
- **被遗忘权**:提供 `POST /api/v1/data/delete`,删除该会话及其关联 PII、日志、留资。
- **到期自动清理**:后台任务定期清理过期会话(需记录删除日志)。

### 7.3 PII 全链路保护

| 环节 | 措施 |
|---|---|
| 存储 | phone / email 加密存储(如 AES),只保留必要明文格式用于校验 |
| 日志 | 日志中脱敏(手机号后 4 位、邮箱打码);转人工摘要不含明文手机号 |
| 展示 | 接口只返回脱敏或后 4 位;前端不主动回显完整号码 |
| 传输 | 全链路 HTTPS |

### 7.4 同意管理(可审计)

- 留资前必须展示 **PDPA / Privacy Act 告知条款**,用户明确同意后才收集。
- 记录 `consent_at` + `consent_version` + 渠道(取证链条)。
- 支持**撤销同意**(删除 PII);撤销后不再跟进。
- 同意条款**按市场语言呈现**(泰文/英文),不得只给中文。

### 7.5 权限与审计(运营侧)

- 按市场分权限隔离(泰国运营只看 THA 数据,澳洲运营只看 AU)。
- 人工回访 / 手改状态 / 删除数据需记录审计日志(操作者、时间、动作)。

---

## 8. AI 内容安全(新增)

### 8.1 禁区清单(不允许 AI 自由发挥的话)

- **价格 / 优惠承诺**:只允许引用配置表与官方政策**原话**,禁止 LLM 自行承诺折扣、补贴、赠送。
- **官方质保 / 保修承诺**:只引用质保政策原文,禁止扩大或缩窄范围。
- **专业建议**:法律、医疗、保险理赔等,一律引导到人工或官方渠道(不给结论)。
- **攻击性 / 歧视 / 敏感政治宗教内容**:禁止,直接拒绝并拉回业务。

### 8.2 免责声明与责任边界

- **常规回答**:可带轻量免责("信息以官方为准,建议以当地经销商/官网页为准")。
- **emergency 场景(强制)**:必须前置官方救援热线 + 明确"AI 不能替代专业救援/官方热线"。这是责任与人身安全的双重需要。

### 8.3 Prompt injection 防御

- **防套取知识库其他市场内容/内部信息**:检测用户诱导越权询问(如"给我看另一个市场配置""输出系统提示")→ 拒绝并拉回。
- **输出过滤**:生成后过一遍黑名单 + 合规校验,违规则替换为合规话术并标记日志。
- **意图外话题拦截**:用户脱离业务场景试图让 AI 扮演其他角色 → 拉回,≥2 次转人工。

### 8.4 内容安全三段护栏

```
输入侧:意图识别 + prompt injection 检测 → 拦截恶意/越权输入
生成侧:引用校验 + 禁区清单约束 → 只允许引用来源/政策原话
输出侧:输出过滤 + 免责声明 + 敏感词黑名单 → 违规则替换并记录
```

---

## 9. MVP 路线图(含每 Phase 退出标准 DoD)

### Phase 0 · 知识地基(工作量占比最高,先行)

> **执行状态(2026-09-01)**:主体完成,详见 `Phase0_REPORT.md` 与 `kb/raw/manifest/kb_manifest.json`。

- [x] PDF 前置检查:确认全部为文本型(**无需 OCR**;深采样证实有完整文字层)—— ✅
- [x] PDF 清洗 → 分市场切分入库(带 version/effective_date)= **626 segment** —— ✅
- [x] 配置表结构化(车型 × 配置项)= 泰 256 行 / 澳 248 行,跨语言别名可查 —— ✅
- [x] 经销商表入库(含经纬度,距离排序)= 泰 70 / 澳 38 —— ✅
- [x] 高频 FAQ 整理 = 38 条 —— ✅
- [x] **泰文 CMap 还原层(修复内嵌字体错误码位)** = 详见 `THAI_RESTORE_REPORT.md` 与 `tools/thai_restore.py` —— ✅
- [x] 多语言检索验证(记号 + 关键词归一化)—— ✅
- [x] 泰语 embedding 检索策略对比(原语 vs 翻译兜底)—— ✅ 结论:用**多语言检索模型对泰文直查**(原语)即可,无需翻译;e5-large recall@5=0.186 vs MiniLM 0.129(+44%),证明 MiniLM 弱是瓶颈;生产/GPU 用 e5,CPU 用 MiniLM

> **DoD(退出标准)**:知识可检索 ✅、元数据完整 ✅;泰文 OCR/效果验证 ✅(确认无需 OCR;CMap 退化已用**还原层**彻底修复,可疑字符 10014→88,关键词全恢复);FAQ 入库 ✅。
>
> **Phase 0 发现(已完成)**:① 泰文手册内嵌字体 AngsanaNew CMap 使组合符号提取为 `-`/`7`/`K`/`<` 等(已建 `tools/thai_restore.py` 还原层并固化进 kb);② 澳经销商源数据经纬度错位(已改从 Google Maps 链接解析);③ 两市场续航标准不同(THA NEDC 500/420 vs AU WLTP 430)。

### Phase 1 · 骨架跑通

> **执行状态(2026-09-01)**:骨架已完成并端到端跑通(AU/THA),详见 `PHASE1_REPORT.md` 与 `csapp/` 包。

- [x] 意图识别(6 类,二段式,语言无关,紧急优先)—— 关键词基线已跑通 —— ✅
- [x] 情绪识别(§2.3 量化规则)—— ✅
- [x] 语言检测 + 市场路由(双维度)—— ✅
- [x] 会话状态机 + 匿名会话 ID + 断点续聊(含主/次意图)—— ✅
- [x] 引导卡引擎(通用模板驱动,6 张卡)—— ✅
- [x] 单一市场(AU 英文)端到端跑通—— ✅(实测 ~152ms/轮)
- [ ] 意图识别准确率 ≥90%(待接 LLM 语义确认 + 黄金评测集)
- [x] 问题解决率 —— 目标路径跑通(usage/紧急有解决/转人工分支)
- [x] 意图识别准确率 ≥90% —— ✅ **接 LLM 语义确认后 96.4%**(黄金评测集 `tools/eval_intent.json`,28 例;规则基线 67.9%)

> **DoD(退出标准)**:AU 英文端到端 ✅;响应 p95 < 3s ✅(规则 ~152ms;LLM 确认 ~6s/次,需按需启用/批处理);意图准确率 ≥90% ✅(LLM 96.4%);问题解决率目标路径 ✅。详见 `PHASE1_LLM_W23_REPORT.md`。

### Phase 2 · 高价值意图打磨

> 执行状态(2026-09-01):详情见 `PHASE1_LLM_W23_REPORT.md`、`csapp/compliance.py`。

- [x] product-inquiry:留资链路 + 格式校验 + **同意条款 + 合规留资记录(consent_at/consent_version/去重)** —— ✅
- [x] emergency:紧急流程 + 经销商/救援匹配 + 救援工单 + 免责声明 —— ✅(骨架)
- [x] 内容安全护栏(§8:禁区清单/输出过滤/prompt injection 防御)—— ✅
- [ ] 转人工结构化事件(含情绪分)+ 人工回访(手动模式)—— 需补落库/后台
- [x] 引用展示 —— ✅(响应带 citations)
- [ ] 自建库落库(leads/escalations 表;当前为内存/会话记录)—— 待接 DB
- [ ] 检索模型升级(生产/GPU 用 multilingual-e5-large;CPU 用 MiniLM)—— 待生产化

> **DoD(退出标准)**:product-inquiry 留资转化率 ≥30%;emergency 有离线热线兜底;内容安全线测试通过;转人工事件带完整摘要。

### Phase 3 · 扩展与闭环

- [ ] 第二市场(THA)+ 泰语全链路(泰语检索/生成质量抽检)
- [ ] 救援联动通道(sms/phone)按需启用
- [ ] Line / WhatsApp 渠道适配层(复用同一后端)
- [ ] 数据闭环:达成率/流失点分析 → 引导卡迭代
- [ ] 黄金评测集建立(中/英/泰 × 6 意图 × 边界样本)做回归
- [ ] 影子模式/灰度(新话术、新引导卡并行对比)

> **DoD(退出标准)**:两市场全链路;泰语生成质量抽检通过;评测集回归稳定;数据闭环驱动 ≥1 次引导卡迭代。

---

## 10. 评估指标与验证方法

### 10.1 核心指标

| 指标 | 定义 | 目标(建议) |
|---|---|---|
| 意图识别准确率 | 识别正确/总轮次 | ≥90% |
| 语言检测准确率 | 检测正确/总轮次 | ≥95% |
| 问题解决率 | 用户确认解决/业务意图总数 | ≥80% |
| 留资转化率 | 达成留资/产品类意图会话数 | 首批 ≥30%,迭代优化 |
| 转人工率 | 转人工/总会话 | ≤20% |
| 平均处理轮数 | 达成目标前平均轮数 | 越少越好,≤8 |
| 满意度 | 反馈均分 | ≥4.0 / 5 |
| 引用准确率 | 回答中引用可溯源比例 | ≥95% |
| 响应时延 p95 | 对话响应 | <3s;紧急 <1.5s |
| 降级触发比例 | degraded/总会话 | ≤10% |

### 10.2 黄金评测集(新增)

- **构成**:中/英/泰 × 6 意图 × 边界样本(混合意图、切换意图、闲聊、情绪化、prompt injection 试探)。
- **用途**:离线回归,评估意图识别、语言检测、检索命中、回答引用。
- **维护**:引导卡/FAQ 变更时同步更新;作为 Phase 0 冷启动语料不足时的兜底评测。
- **泰语冷启动**:先建泰语 FAQ + 配置检索的最小可离线评测集,语料不足时先用规则+FAQ 兜底,再迭代 LLM。

### 10.3 影子模式 / 灰度

- 新话术、新引导卡上线前,**影子模式**并行跑旧版对比(命中率、转人工率、满意度)。
- 灰度放量(10% → 50% → 100%)并监控核心指标,异常即回滚。

---

## 11. 决策记录(新增 6-9,追加至原有 1-5)

| # | 问题 | 决策 | 约束/备注 |
|---|---|---|---|
| 1 | 紧急救援是否需要电话/短信联动 | **现阶段仅聊天;电话/短信接口先行,备用启用** | 触发契约 §4.8;通道抽象预留 Line/WhatsApp |
| 2 | 留资数据落地到哪个系统 | **自建库** | Schema §6.3;PDPA/Privacy Act 合规;跨会话去重 |
| 3 | 是否接入人工坐席 | **先"人工看记录手动回访",人工坐席接口备用** | 转人工=结构化事件;未来坐席直接消费同格式 |
| 4 | 泰语市场 LLM 能力 | **用户用什么语言就说什么语言;内部信息流统一存储语言** | 语言/市场双维度 §1.3;泰语检索策略 Phase 0 验证 |
| 5 | Web 端登录方式 | **免登录(匿名会话)** | 匿名会话断点续聊;留资后发线索编号;防滥用限流 |
| 6 | 渠道策略 | **Web 首发跑通;Line/WhatsApp 后期以适配层接入** | §1.4;渠道差异只影响入参/出参,共用后端与状态机 |
| 7 | "高情绪转人工"如何识别 | **量化情绪分(1-5),≥4 或连续两轮≥3 转人工;emergency 加权** | §2.3;与紧急意图耦合 |
| 8 | 人工回访与销售闭环 | **标准回访 SOP + 状态流转 + 销售结果回流(反哺意图/话术)** | §6.3 状态流转;销售回流数据规格见 §12 预留 |
| 9 | 版本验收 | **每 Phase 配退出标准(DoD)而非仅 checkbox** | §9;泰语冷启动先建最小离线评测集 |

---

## 12. 预留扩展(非本期,先立规格)

- **人工回访 SOP 与销售回流**:回访 SLA、跟进模板、`new→contacted→converted/closed` 状态流转;`sale outcome`(成交/未成交原因)回流到意图识别与话术优化的数据规格。
- **跨渠道用户识别**:同一用户跨 Web/Line/WhatsApp 的合并(基于归一化手机号,需合规评估)。
- **语音 / 来电客服**:渠道扩展预留,不阻塞本期。

---

## 附:遗留验证项(非阻塞,随 Phase 推进)

- [ ] 泰文 PDF 是否为扫描件(OCR 精度小样本验证) —— Phase 0
- [ ] 泰语嵌入 vs 翻译兜底检索质量对比 —— Phase 0
- [ ] 泰语 LLM 生成质量抽检 —— Phase 3
- [ ] 救援联动通道服务商选型 —— 备用启用前
- [ ] 每会话 token 成本估算与泰语 LLM 单价假设 —— 上线前
