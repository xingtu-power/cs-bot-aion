# 2026-09-08

## 工作摘要

### 知识缺失兜底全局方案（knowledge-gap-fallback）

- 用户反馈：截图里"泰国市场卖哪些车型"被 LLM 答成"暂无清单、建议联系经销商"，无留资引导。
- 现状：策略碎片化、互相矛盾、缺多语言、缺后处理保险。
- 方案：分"前-中-后"三层 + 配套改动（详见 `docs/plans/knowledge-gap-fallback.md`）
  - 第 1 层：6 张卡片 state_desc 全部规范化 + llm.respond 全局 prompt 加 KNOWLEDGE-GAP POLICY
  - 第 2 层：intent.is_knowledge_gap 4 语言正则检测 + pipeline._knowledge_gap_lead 多语言模板 + 主流程后处理
  - 第 3 层（中长期）：kb/THA, kb/AU 建 models.json + kb.context() 注入
  - 配套：compliance._FAB_REPL 多语言化、前端"📞 留资"按钮（可选）
- 影响范围：全 6 个意图，全 4 语言

### 流程纠正

- 用户两次纠正："先出现状分析 + 方案讨论再动手"、"动手前先拉分支"——已记 user-level memory。
- 上一次 cc363b2 提交已 revert（commit `b33140b`），分支 `feat/knowledge-gap-fallback` 已建（基于 b33140b，工作区干净）。

### 文件

- 新增方案 MD：`docs/plans/knowledge-gap-fallback.md`
- 更新 user memory：`~/.workbuddy/MEMORY.md` 加"文档输出习惯"小节（方案类生成 MD）

### 实施

- 用户拍板：1.x + 2.x + 4.1 一并改，3.x 短期不做，4.2 按钮不要
- 在 `feat/knowledge-gap-fallback` 分支上按 Phase 实施，每个子项独立 commit（共 7 commit）:
  1. `a1ed603` feat(cards): Phase1.1 — 6 张卡片 desc 规范化 KNOWLEDGE-GAP FALLBACK
  2. `dad9466` feat(cards): Phase1.2 — talk_scripts.json 同步
  3. `cc3446c` feat(llm):  Phase1.3+1.4 — 全局 prompt KNOWLEDGE-GAP POLICY + MODEL ACCURACY 升级
  4. `7f24683` feat(intent): Phase2.1 — is_knowledge_gap + already_pitching_lead 4 语言检测
  5. `f12d349` feat(pipeline): Phase2.2+2.3 — 知识缺失兜底后处理
  6. `124b2f3` feat(pipeline): Phase2.4 — _fallback_text 同步
  7. `6e16831` feat(compliance): Phase4.1 — guard_model_facts 多语言化
- `ab88a2b` docs: 更新方案实施清单与验收结果
- HEAD = `ab88a2b`

### 验收

- 14 个单元测试全部通过（5 个追加 + 3 个不重复 + 3 个非售前类 + 2 个正常回答 + 1 个非 UT 负例）
- 3 个 E2E curl 验证：
  - `泰国市场卖哪些车型` (zh, product-inquiry) → 回复含留资引导 ✅
  - `how do I charge` (en, usage-guide) → 无留资引导 ✅
  - `my car wont start` (en, emergency) → 无留资引导 ✅
- rule 模式在 `http://127.0.0.1:8020` 跑（task `rNjUJJ`）

### 待用户决策

- 合并到 main？
- deepseek 模式跑真实问答（需要 key）
- Phase 3（数据补全）何时启动
- Phase 4.2（前端按钮）是否还要