# cs-bot-aion 变更历史与修改摘要

> 本文档汇总本项目（AION 海外智能客服 · GAC 广汽国际）**历来的所有修改**，按**阶段/能力**归类，便于回溯与评审。
> 代码仓库：https://github.com/xingtu-power/cs-bot-aion · 当前 `main` 包含自首个基础提交以来全部 43+ 提交。

---

## 代号与主线

- **客户**：广汽国际（GAC Global，总部），`AION`、`昊铂Hyper` 是其**子品牌**。
- **产品**：RAG 智能客服，Python 3.9；**每轮一次 LLM 调用**（准确率高优先）；多语言（中/英/西/泰，回复跟随下拉框语言）；卡片状态机；按市场路由（THA/AU 有知识库，其它→AU）。
- **目标**：以**准确、一致性、不臆造**为核心，售前（回答→引发购买兴趣→留资）、售后（解决→确认满意，不推销售、不轻易转人工）。

---

## 一、早期核心（仓库最初阶段）

| commit | 主题 |
|---|---|
| `9d65be3` | 产品文档更新至 v0.4(as-built) + 清理遗留备份 |
| `a69e504` | 纳入向量索引 `.npz`（避免启动重建） |
| `9f062cb` | 回复质量护栏 + 槽位追问护栏 + 语言跟随修复 + 文档 |
| `1a98b80` | 品牌合规 + 话题边界/丝滑拉回（跑题自然桥接回 AION） |
| `6947060` / `170e5ec` | 前端四语言化 + 语言下拉 + 布局精简 |
| `cdacaf1` | 离线 E5 镜像构建 + 上线流程（部署） |
| `748dbd9` | 前端去掉状态栏（意图/步骤/目标/延迟行） |
| `4754a92` | 槽位/紧急意图粘死不切 + 输入法组合误发送修复 |
| `b44c949` / `6cfbcb4` / `cec9dbc` | 经销商注入真实门店（名称/地址/坐标）+ 城市别名跨市场匹配 + 主动提供经销商电话（公共信息）+ 防臆造库外门店 |
| `de47c72` | 回复语言跟随**下拉框所选**（`lang_hint`），不再跟消息/上次语言 |
| `7472c33` | 意图限定 6 类 + 短确认续接 + 延续标签 + 文字来源引用 + parse 鲁棒 + 救援引导 + 文档 |

> 注：提交历史中 `7472c33` 之后出现多条**同名**"意图限定…+ 文档"提交（`4fe89fc`…`eb38648`），是协作过程中 re-apply/历史整理痕迹，非新增功能。

---

## 二、本轮会话/转人工/引导（核心迭代）

### 2.1 会话与身份（`42e3fa6` 为主）
- **非匿名身份**：前端 localStorage 自动生成/恢复 `userId`；`/api/v1/chat` 携带 `userId`；服务器透传并关联会话（宿主透传 `window.__DSH_USER__.id` 亦支持）。
- **会话结束策略**：`ended / ended_at / ended_reason`（goal|escalated|goodbye|idle）；空闲超时 `TTL_RECENT=1800`；存档 `ARCHIVE_DAYS=90` 启动清理；`END_NOTICE` 4 语言。
- **历史会话端点**：`GET /api/v1/sessions?userId=`（摘要列表）、`GET /api/v1/session?sessionId=`（含 history），前端"新会话按钮 + 历史列表"。

### 2.2 转人工策略（反复确认、不轻易切、高门槛）
- **放宽容忍**：`SLOT_MAX_ASK 3→4→8`、`CLARIFY_MAX_ROUNDS 2→3`。
- **情绪≥4 才触发**：`EMOTION_ESCALATE_SCORE=4`（启用情绪路径），确认后再转。
- **转人工前先确认**：三条转人工路径（槽位超限/澄清超限/情绪超标）都先问"是否转人工"，**用户确认才真转**；拒绝则不转、继续陪伴。
- **主流程"反复确认"= 纠正无效信息 + 重问**：用户给的信息格式/类型不对 → bot 自然点出问题并要正确信息（每次），而非"需要转人工吗"。
- **高门槛**：`SLOT_MAX_ASK=8`，仅连续多次提供不了有用信息（敷衍/无效）才触发确认转人工。
- **自然话术**：确认/转接话术改用 **LLM 生成**（`_nat`），弃用硬编码生硬模板（温暖模板兜底）。
- **`_is_confirm` 否定修复**：`不需要/不用了/不要人工` 不再被当成确认转人工。

### 2.3 语义槽位 / 意图稳定
- **语义感知计数**：`_is_cooperative`——用户在提供有用信息（城区/时间/电话尝试/预约意向）→ 重置计数、不升级；只有敷衍/答非所问才累计。
- **concern / confirm 语义判断**：给了实质偏好（如"我想要黑色的车"）不算失败；"还是不行/还有问题"→ 继续排查、不升级。
- **纯问候强校正**：`intent.greeting()` 把 LLM 偶发把问候误判成业务意图归回 `other`。
- **LLM 温度降至 0.3**：更确定、少飘移。
- **空泛反问兜底（收紧版 A）**：仅"首轮 + 业务意图 + 命中空泛反问"时，用严格指令**二次调用**直接作答一次；其余单次调用。（`aaa0eaf`）

### 2.4 引导话术外置（`42e3fa6`）
- 每意图的**引导卡**（每步 `desc/expect/advance/goal` 及喂给 LLM 的英文 `state_desc`）抽到 **`csapp/talk_scripts.json`**：改话术只需编辑该文件 + 重启，无需改代码；`cards.py` 内置默认兜底。
- 6 类意图按**售前/售后目标**编写：
  - 售前（product-inquiry / dealer-lookup）：先回答→匹配需求购买点→引发兴趣→引导留资/预约。
  - 售后（usage-guide / emergency / after-sales）：先解决→确认满意/给下一步；不推销售、不约到店/要联系方式。

### 2.5 会话结束语义 v0.5（分 PR 合并）
- `GOODBYE_RE` 扩为**多语言**（中/英/泰/西，含"谢谢/感谢/暂时不需要/算了"，词边界+结构防误判）。
- 六类 `ended_reason`：goal / escalated / goodbye / **user_closed** / **idle** / **no_progress**。
- `Session.end(reason)`、`StateStore.mark_ended`、`idle_mark(ttl)`。
- `POST /api/v1/session/end`（幂等）+ `main()` 后台 daemon 每 60s `idle_mark`。

---

## 三、知识库 / 引用 / 配图

### 3.1 引用与知识溯源
- **引用页码**：每条引用显示 `文档名 + 页码`（去重、限量 3 条）；正文注入 `[来源: doc Ppage]`。
- **跨标准规格参考（方案 A）**：同一车型（仅 AION UT）在不同市场用不同测试循环标注，bot 报**全标准、不否认任一标准**：
  - 续航 **WLTP 430（AU）/ NEDC 500(500 Premium) / NEDC 420(420 Standard)（THA）**；电池 60 / 50.27 kWh；充电 11kW / 24min。
  - 数据源：**仅 AU/THA 规格表**（不引中国 CLTC）；`csapp/kb.py` 的 `_MODEL_SPECS`。

### 3.2 回答配图（必要时才展示，只作辅助）
- 知识库页内**真实插图**抽取：`tools/extract_images.py` 用 PyMuPDF 裁剪页内图区域（过滤 logo/图标/分隔线），存 `kb/<mkt>/images/<v>_p<p>__f<i>.jpg`（JPEG Q90）。
- `answer_images` **仅 `usage-guide`/`emergency` 且命中内容页时**返回；规格/经销商/政策/闲聊不配图（图片只作辅助，文字先详细作答）。
- 跳过 目录/索引/前言 等非内容页；按"操作/步骤"倾向评分选页；服务端点 `GET /api/v1/kb/image` 防目录穿越。
- 配图目录 `kb/*/images/` 已入库（JPEG ~13.8MB），新机器 clone 即含。

### 3.3 车型防臆造（重点）
- **`guard_model_facts`**：非 UT 车型出现其未提供的详细参数（功率/扭矩/电池容量/马力等）→ 该句强制替换为"该参数暂无确切信息，建议联系授权经销商/官方热线"。UT 因有完整 facts 不受影响。
- `MODEL LINEUP` 全系推荐 + `SPEC`/`ACCURACY` 规则：只引 facts 中真实存在的值、不跨车型套数、不改测试循环标签、不提及未列车型、缺失说"暂无"。

---

## 四、本轮反馈优化（GAC 品牌 + 答复质量）

### 4.1 品牌为 GAC（`9ad2649`）
- 界面标题/问候/提示词改为 **GAC（广汽国际）**；`AION`、`昊铂Hyper` 为 GAC 子品牌。
  - 前端问候："我是 **GAC 智能助手**，GAC 旗下有 AION、昊铂 Hyper 等品牌…"；标题"GAC 海外智能客服"。
  - `llm.py`：`You are a GAC (广汽国际) customer-support bot serving the AION and Hyper brands`。
  - `other` 卡自我介绍改 GAC；`MODEL LINEUP` 头部标注"GAC 广汽国际 · AION + 昊铂Hyper"。

### 4.2 去负面 / 迂回缺点（`9ad2649`）
- `MODEL LINEUP` **去掉所有"缺点:"**，新增 `POSITIVE-FRAMING` 规则：介绍只讲定位/优势；即使被问缺点，也用**正面设计定位**迂回（如"紧凑车型为 1-2 人设计，4+ 空间受限"、"电池按价位定位"），不列硬伤。

### 4.3 规格标准一致（`9ad2649` / `ed61528` / `eaf800c`）
- `SPEC STANDARD` 规则：报续航/性能**注明标准**（WLTP/NEDC/CLTC），**不否认任一标准**（同车不同市场不同测试循环）。
- 实测 AU/THA/CN 三种市场问续航均报"WLTP 430 / NEDC 500/420"，口径一致、无 CLTC。

### 4.4 只答所问（`9ad2649`）
- usage-guide 只给**所问组件/动作**的步骤（尾门 ≠ 前机盖）；facts 未清晰覆盖该组件则说"暂无"，别给别的组件步骤。

### 4.5 经销商计数精准（`9ad2649`）
- 只列 facts 中有的店，**不虚报数量、不留空编号**（修"三家只出 2 家 + 空 3."）。

### 4.6 知识缺口 → 柔性留资兜底（`e0fa47b`）
- **策略**：知识库**没有**的信息（如"泰国市场在售车型清单"）→ **不臆造**，如实说暂无，然后**引导留资**（请留手机/邮箱，由当地 GAC/AION 专员跟进提供准确信息）；不只说"联系经销商"就完事。
- `llm.py`：respond 提示词新增 `KNOWLEDGE GAP` 规则（facts 未覆盖/无该明细 → 留资兜底）。
- `kb.py`：`MODEL LINEUP` 注明"**通用车型概览**（中国定位），**非任一市场在售清单**；问市场在售/明细 = 知识缺口 → 留资，不能拿通用清单当该市场在售"。
- **实测**："泰国市场卖哪些车型？" / "泰国有没有 AION LX？" → 均"暂无确切清单 + 留下联系方式跟进"。

---

## 五、前端 UI（聊天窗演进）

| 迭代 | 说明 |
|---|---|
| 早期四语言化 + 语言下拉 | 中/英/西/泰；回复跟随下拉 |
| 去掉状态栏 | 界面更简洁 |
| (**PR #1 合并, `2de7112`**…) 左右双栏 | 左栏 280px：品牌 + 新会话 + 历史列表（按天分组/状态灰显/已结束不可继续）；右栏聊天窗 + [× 关闭]（调 `/session/end`）；响应式 `<720px` 折叠为顶部抽屉 |
| 后续 UI 提交 | 顶栏用户信息卡片 + 消息头像/名字/时间；用户名简化为"用户"、bot 名"智能客服"；本地时区时间；Web 客服窗填满右侧；默认头像；顶栏副标题随当前会话更新、状态统一"在线·语言"等 |
| 本批反馈 | 双栏版上改为 GAC 品牌 |

---

## 六、其它 / 尚未落实

- **检索按车型/意图过滤（修 citations 全错位）**：用户问昊铂GT/北京却引用 AU UT 手册——单独开盘 `fix/kb-citation-by-model`，高优先。
- **重复追问（cards 推进后 state_desc 仍相同）**：间歇复读，状态机层逻辑，需单独 review。
- **图片扩到 product-inquiry**：用户已明确**不扩**（保持 usage/emergency 才配图）。
- **issue-mode 延续标签**：`is_continuation` 依赖 LLM 自造标签，rule 模式不触发（与意图稳定相关）。

---

## 七、咨询效果分析(独立面板 + 每轮 Trace)

分支:`feat/analytics-trace`。为"用户咨询历史效果分析、反哺优化客服"新增旁路可观测能力,不改变任何对话行为:

- **每轮 trace 采集**(`csapp/trace.py` + `csapp/analytics.py`):`pipeline.chat()` 拆 `_chat()` 并由外层包装
  begin/flush(try/finally,异常不丢);语言/市场/注入/RAG/意图/卡片/护栏等阶段结构化落库
  (含阶段耗时、LLM 原始输出截断、自动问题信号),PII 掩码后入库。
- **独立分析库** `data/analytics.db`(a_sessions/a_turns,保留 180 天,与会话 JSON 90 天清理解耦);
  `tools/backfill_traces.py` 一次性回填存量会话(部分 trace)。
- **独立分析面板** `/admin`(只读;`csapp/static/admin.html`,与访客聊天页零耦合、无入口):
  概览 KPI + 问题信号分布 + Top 知识缺口 → 会话检索 → 会话详情(对话历史回放 + 每轮可展开 Trace 时间线)。
- **访问控制**:设置 `CSAPP_ADMIN_TOKEN` 后 /admin 与 /api/v1/admin/* 需 `X-Admin-Token`;留空仅本机回环可访问。
- **效果信号(确定性,零额外成本)**:llm_failed/llm_retry/llm_parse_fail/empty_reply/knowledge_gap/vague_reply/
  clarify_loop/user_repeat/escalated/guard_*/slow/no_progress,全部可下钻到具体轮次。

---

## 关键文件一览

| 文件 | 角色 |
|---|---|
| `csapp/pipeline.py` | chat 编排、会话结束判定、转人工/情绪/澄清、重答兜底 |
| `csapp/cards.py` | 引导卡状态机、槽位校验/语义、`_nat`、防臆造 `guard_model_facts` 调用、`_is_confirm` |
| `csapp/talk_scripts.json` | 引导话术（外置，可编辑） |
| `csapp/kb.py` | 检索、引用/页码、`answer_images`、`_MODEL_LINEUP`/`_MODEL_SPECS`、跨标准参考 |
| `csapp/llm.py` | `respond` 提示词（品牌/正向框架/规格标准/只答所问/ACCURACY），温度 0.3 |
| `csapp/config.py` | GOODBYE_RE、SLOT/澄清/情绪阈值、TTL/归档、END_NOTICE |
| `csapp/state.py` | Session（end/touch）、StateStore（list/get/mark_ended/idle_mark/cleanup） |
| `csapp/server.py` | `/chat`、`/sessions`、`/session`、`/session/end`、`/kb/image`、后台 idle 巡检 |
| `csapp/analytics.py` | 分析库 a_sessions/a_turns(读写+聚合+PII 掩码),新增 |
| `csapp/trace.py` | 每轮 trace 线程级采集(step/issue/llm_mark/flush),新增 |
| `csapp/static/admin.html` | 独立效果分析面板(/admin,只读,与访客页隔离),新增 |
| `tools/backfill_traces.py` | 存量会话回填分析库(部分 trace),新增 |
| `csapp/static/index.html` | 四语言 UI + 左右双栏 + 历史列表 + GAC 品牌 |
| `tools/extract_images.py` | 页内插图抽取（JPEG Q90） |
| `deploy/` | Dockerfile / compose / 离线 E5 镜像 / 上线脚本 / 说明 |

---

## 七、品牌结构纠正（`feat/brand-structure-fix`，2026-09-08）

### 7.1 三品牌结构纠正
- 品牌结构从「GAC 广汽国际 = 母公司，AION + 昊铂 Hyper 为子品牌」纠正为「**广汽集团 GAC Group** 旗下 **传祺 GAC Motor / 埃安 AION / 昊铂 HYPTEC** 三大并列品牌」。
- 昊铂官方英文名 **Hyper → HYPTEC**。
- 涉及文件：
  - `llm.py`：respond/classify/answer 提示词品牌口径（三品牌并列 + HYPTEC）。
  - `kb.py`：`MODEL LINEUP` 标题（三品牌 + HYPTEC）。
  - `talk_scripts.json` + `cards.py`：other 卡自我介绍三品牌口径。
  - `compliance.py`：留资同意条款（AION / GAC → GAC Group）+ 防臆造正则 `_NON_UT_RE`（Hyper → HYPTEC）。
  - `static/index.html`：四语言问候语三品牌口径。
  - `README.md`：品牌描述同步。
- 方案文档：`docs/plans/brand-structure-fix.md`。
- **待办**：GAC 海外车型清单（外部数据，待提供后补入 `_MODEL_LINEUP` 与 `_NON_UT_RE`）。

### 7.2 品牌①命名 + 转人工归属统一（2026-09-08）
- 品牌①对外名称统一为 **GAC**（不再用「传祺 / Trumpchi / GAC Motor」）。
- 转人工/留资/热线/服务归属统一为 **GAC 专员 / GAC specialist / GAC hotline / GAC service center**（原「AION 专员 / specialist / hotline / service center」约 40 处，覆盖中/英/泰/西四语言）。
- 涉及文件：`llm.py`、`kb.py`、`cards.py`、`talk_scripts.json`、`pipeline.py`、`intent.py`、`static/index.html`、`README.md`。
