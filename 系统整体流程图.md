# 对话系统整体流程图（as-built）

> 覆盖：从用户消息到最终响应的完整链路 + 关键分支（意图、紧急、跑题、他牌、转人工）。
> 图例：圆角矩形=处理步骤；菱形=判断分支；→=流转。

```mermaid
flowchart TD
  A[用户消息 POST /api/v1/chat] --> B[会话恢复 StateStore]
  B --> C[语言检测 detect_language]
  C --> D[市场路由 route_market  THA/AU/CN…]
  D --> E{内容安全 is_prompt_injection?}
  E -- 命中 --> E1[安全回复并退出]
  E -- 否 --> F[知识上下文 kb.context]
  F --> F1[向量 top-k: 文档+FAQ e5-large]
  F --> F2[结构化规格: 7 概念]
  F --> F3[经销商: 名称/城市/坐标匹配 + 电话]
  F --> F4[紧急: 救援热线/SOS]
  F --> G["一次 LLM respond: intent+question+response<br/>意图限 6 类 · 语言=下拉框 · 短确认接续"]
  G --> H{意图规整}
  H --> H1[emergency_hit 强制 emergency]
  H --> H2[延续标签 is_continuation → 保留当前意图]
  H --> H3[normalize_intent → 标准 6 类]
  H1 & H2 & H3 --> I{意图路由}
  I -->|product-inquiry| P["车型咨询 3 步: 答→concern→留资"]
  I -->|dealer-lookup| DL[经销商 1 步: 门店+预约+留资]
  I -->|usage-guide| UG[使用方法 1 步: 步骤→确认]
  I -->|emergency| EM[紧急 1 步: 安全→救援同意→工单]
  I -->|after-sales| AS[售后 1 步: 答+约到店/留资]
  I -->|other| OT[问候/引导/回 AION]
  P & DL & UG & EM & AS & OT -- run_card 校验/推进/留资/救援 --> J["回复质量 _polish_reply<br/>禁AI感/去重/长度"]
  J --> K{输出过滤 + 他牌}
  K -- 他牌/竞品 --> K1[知识缺失+留资引导]
  K -- 通过 --> L[落库 db + session.persist + append_turn]
  L --> M[debug.record turn]
  M --> R["响应 (reply, intent, language, citations, consentVersion…)"]
```

## 关键分支说明
| 分支 | 判断 | 去向 |
|---|---|---|
| 紧急 | `emergency_hit` 强信号 | 强制 `emergency` 卡（安全确认 → 救援同意 → 工单） |
| 延续 | LLM 返回 `accept_offer_continue/agree/确认` 等 | 保留会话当前业务意图，接上刚才话题 |
| 跑题/无关 | 天气/新闻/生活等 | re-anchor 桥接回 AION |
| 他牌/竞品 | `find_competitor` | 只推 AION + 知识缺失留资引导 |
| 意向不明 | 意图低置信、非业务 | 澄清(A/B/C/D) → 超限转人工 |
| 槽位收不到 | `SLOT_MAX_ASK` | 转人工一次 |
| 知识检索 | 各数据源 | 文档/FAQ(向量) + 规格(精确) + 经销商(名称/城市/坐标) + 紧急(救援) |

## 一句话
**消息 → 会话/语言/市场/安全 → RAG(向量+规格+经销商+救援) → 一次 LLM(意图+回答, 下拉语言) → 规整(延续/紧急) → 卡片状态机(推进/留资/救援) → 话术质量/过滤/他牌 → 落库持久化 → 响应(含来源引用)**。
