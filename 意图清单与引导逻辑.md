# 意图清单与引导对话逻辑（as-built）

> 说明：**意图判定**由一次 LLM `respond` 完成（限定 6 类）；**引导逻辑**由 `cards.py` 的卡片状态机定义（每一步的 `state_desc` 英文任务指令 + `expect` 校验），**用户看到的话术由 LLM 按下拉语言 + facts 生成**。下表"示例引导话术"是各步 LLM 生成的**中文示例**（非固定模板）。

---

## 意图总览（6 类）

| 意图 | 触发 | 主目标 |
|---|---|---|
| `product-inquiry` | 用户问 AION UT 车型/配置/参数/价格 | 回答 + 留资 |
| `dealer-lookup` | 找经销商/门店/试驾预约 | 给门店 + 留资 |
| `usage-guide` | 怎么用/怎么充电 | 给步骤 + 确认解决 |
| `emergency` | 紧急/抛锚/无法启动 | 安全 + 发救援工单 |
| `after-sales` | 售后/质保/维修 | 回答 + 约到店/留资 |
| `other` | 打招呼/闲聊/跑题/短确认 | 问候/引导回 AION |

> 紧急强信号（`emergency_hit`）会**强制** emergency；跑题会 re-anchor 回 AION；他牌/竞品会**只推 AION + 引导留资**。

---

## 1. `product-inquiry`（车型咨询）— 3 步

| 步 | state_desc（英文引导指令） | expect | 校验 | 推进/达成 |
|---|---|---|---|---|
| 0 | Answer the customer's AION UT product/spec question using the facts. | question | 任意输入 | → 推进到步1 |
| 1 | Ask what matters most (range / space / price), then move toward connecting them to a dealer. | concern | 命中 result 关键词（range/space/price 等多语言） | 收集 concern → 推进到步2 |
| 2 | Ask the customer to leave a phone or email (with privacy consent) so a local dealer can connect. | contact | 收到有效 7-15 位手机号 或 含@邮箱 | **达成(goal)** + 留资 |

**示例引导话术**（中文，LLM 生成）：
- 步0："AION UT 综合续航 430 公里（WLTP），电池 60kWh…"
- 步1："您更关心续航、空间还是价格？我可以帮您对接本地经销商。"
- 步2："麻烦留一下电话或邮箱，本地经销商方便联系您安排看车。"

---

## 2. `dealer-lookup`（经销商 / 试驾预约）— 1 步

| 步 | state_desc | expect | 校验 | 达成 |
|---|---|---|---|---|
| 0 | Share the nearest dealers (from facts) and offer to book a visit. Ask for a contact to arrange it. | contact | 有效电话/邮箱 | **达成(goal)** |

**示例引导话术**："GAC สีลม ซอย 9 在曼谷，地址…，电话 0830668577。要帮您预约试驾吗？方便留个联系方式？"
> 给真实门店（名称/地址/电话，跨市场+城市别名匹配）；一直收不到联系方式 → SLOT_MAX_ASK 后**转人工一次**。

---

## 3. `usage-guide`（使用方法/充电）— 1 步

| 步 | state_desc | expect | 校验 | 达成 |
|---|---|---|---|---|
| 0 | Give the steps to solve the customer's problem, then ask if it was resolved. | confirm | 命中 yes/已解决/ได้/解决了 等 | **达成(goal)** |

**示例引导话术**："交流充电：解锁后按压充电口盖后缘…直流快充约 24 分钟。您的问题解决了吗？"

---

## 4. `emergency`（紧急救援）— 1 步

| 步 | state_desc | expect | 校验 | 达成 |
|---|---|---|---|---|
| 0 | Confirm the customer is safe. Give the AION roadside-assistance hotline from the facts (state the exact number if present; if none, point them to the Emergency Rescue Guide). Then offer to dispatch a rescue ticket and ask for consent. | consent_rescue | 命中 同意/是/ส่ง/帮忙 等 | **达成(goal)** + 救援工单 |

**示例引导话术**："请先确保您和乘客安全。请按 SOS 键 2–5 秒呼叫救援中心（或查看《紧急救援手册》热线）。需要我为您生成救援工单吗？"

---

## 5. `after-sales`（售后/质保）— 1 步

| 步 | state_desc | expect | 校验 | 达成 |
|---|---|---|---|---|
| 0 | Answer the customer's service/warranty question (from facts); offer to book a visit or get a contact. | contact | 有效电话/邮箱 | **达成(goal)** |

**示例引导话术**："AION UT 质保条款以官方政策为准，具体请查车主手册。需要我帮您预约到店，或留个联系方式？"
> 缺：无"复杂问题→人工/WhatsApp"分支（见设计缺口）。

---

## 6. `other`（打招呼/闲聊/跑题/短确认）— 1 步

| 步 | state_desc | expect | 校验 | 推进 |
|---|---|---|---|---|
| 0 | Greet, introduce yourself as the AION assistant, and guide the customer into a topic. | none | 无 | 不推进(advance=0) |

**示例引导话术**："我是 AION 海外智能助手。可以问我配置、经销商、使用、售后或紧急救援。点击下方示例快速体验。"

**特殊行为**：跑题（天气/新闻等）→ 自然桥接回 AION；用户对上一轮提议回"好的/是" → **接上那个话题**（LLM 判对应业务意图）；他牌/竞品 → 只推 AION + 引导留资。

---

## 通用机制
- **进卡**：`respond` 意图 → `normalize`（含延续标签保留当前意图）→ 已锁定走当前卡，否则 `lock_intent`。
- **推进/校验**：`run_card._check` 按 `expect`（question/concern/contact/confirm/consent_rescue/none）。
- **话术生成**：每步 `state_desc` 注入 LLM → LLM 按**下拉语言 + facts** 生成回复。
- **转人工**：槽位超 `SLOT_MAX_ASK`（转人工一次）/ 意图澄清超限 / 情绪超标。
- **知识检索**：文档/FAQ(向量)+规格(精确)+经销商(名称/城市/坐标)+紧急(救援热线)。
