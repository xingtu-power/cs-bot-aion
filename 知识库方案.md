# 知识库方案(cs-bot-aion 现行实现)

> 说明：本文描述当前 **cs-bot-aion** 知识库的**落地实现**（as-built），覆盖 原始资料处理 / 切分 / 索引 / RAG / 上下文 五个环节。文件、规模、检索方式均以实际代码与 `kb/` 数据为准。

---

## 0. 总览

```
原始资料(泰国/澳洲 手册PDF·配置表xlsx·经销商xlsx)
   │  ETL(manifest 驱动)
   ▼
kb/{THA,AU}/
 ├── docs/         车主手册/救援/快速指南  (JSONL, 按页切分)
 ├── spec/         车型配置  (specs.jsonl + spec_index.json)
 ├── dealers/      经销商    (dealers.jsonl)
 ├── faq/          FAQ       (faq.jsonl + faq_localized.json 4语言)
 ├── vectors/      向量索引  (vectors.npz + ids.json + texts.json)
 └── ../raw/manifest/  来源清单 + 提取摘要
```

两类数据**各走不同的检索**：
- **自由文本（文档/FAQ）** → **向量语义检索**（e5-large）。
- **结构化/实体（规格/经销商）** → **精确字段查询 / 名称·城市·坐标匹配**（非向量）。

---

## 1. 原始资料处理方案（ETL）

**源**（输入，构建期，运行时不依赖）：
- `data/泰国智能客服训练材料-260515/`、`data/澳洲智能客服训练材料-260811/`
- 内含 AION UT **车主手册(PDF)**、**产品配置表(xlsx)**、**经销商表(xlsx)**。

**处理链**（`tools/`，由 `kb/raw/manifest/sources.json` 统一驱动）：

| 工具 | 作用 | 产物 |
|---|---|---|
| `extract_pdf.py` | 用 PyMuPDF **逐页**抽文本，CMap 泰文声调还原 | `kb/<market>/docs/<version>.jsonl` |
| `etl_config.py` | 配置表 → 规格字段 + 多语言别名索引 | `spec/specs.jsonl` + `spec_index.json` |
| `etl_dealers.py` | 经销商表 → 门店结构化 | `dealers/dealers.jsonl` |
| `build_faq.py` / `build_faq_localized.py` | 提炼 FAQ + **4 语言烘焙** | `faq.jsonl` + `faq_localized.json` |
| `thai_restore.py` / `apply_thai_restore.py` | 泰文 CMap 声调退化修复 | 文档内容修复 |
| `embed_index.py` | 离线构建向量索引 | `vectors/`(npz/ids/texts) |

**要点**：
- 知识内容**本地化**（非运行时 LLM 翻译）：FAQ 4 语言、规格字段多语言标签、文档泰文还原。
- 各数据带 `manifest` 记录**来源/版本/生效日期/页数/字数**，支持溯源。

---

## 2. 切分方案（chunking）

| 数据 | 切分粒度 | 说明 |
|---|---|---|
| **文档** | **按页**（1 页 = 1 chunk） | `id=市场-类型-版本-p0001`，含 `page_no/content/chars/cmap_restored`。THA 车主手册 **221** 块，**avg ≈1358 字符**（min 143 / max 2751） |
| **FAQ** | **1 条 = 1 chunk** | 问 + 答作为一个对象 |
| **规格** | **不切分** | 结构化字段查询 |
| **经销商** | **不切分** | 结构化实体 |

> 文档 chunk 以**页**为单位，粒度偏大（单块可达 ~2.7k 字符）；对长页可考虑**再按小节/段落二次切分**，目前保持页级。

---

## 3. RAG 方案

- **检索（retrieve）**：`kb.context(query, lang, topk)` 拼接：
  1. **向量语义 top-k**（文档 + FAQ，主语言）；
  2. **结构化规格**（7 个概念固定追加）；
  3. **经销商注入**（`dealer_context`）；
  4. **FAQ 关键词兜底**（向量未命中时）。
- **生成（generate）**：`_llm.respond(message, context, state_desc, conv_lang, history)` —— **一次 LLM 调用**输出 `{intent, conf, question, response}`。
- **特点**：**检索 + 生成合一（单次调用）**；**回复语言 = 下拉框所选语言**（`CSAPP_LANG` / `lang_hint`）；意图与回答一次产出。

---

## 4. 索引方案

### 向量索引（文档 + FAQ）
- 模型：`intfloat/multilingual-e5-large`（**1024 维**，多语言）。
- **离线构建**：`tools/embed_index.py` → `kb/<market>/vectors/vectors.npz` + `ids.json` + `texts.json`。
- **在线查询**：单条 query 编码（`query: …`）→ 与索引矩阵做**余弦相似度** top-k。
- **共享单例 + 启动预热**：模型模块级单例，`kb.warmup()` 启动加载，避免首条消息付模型加载成本。
- **规模**：THA **274** 块、AU **390** 块（×1024）。
- **锁定依赖**：`fastembed==0.8.0`（与内置索引的 mean pooling 匹配；**不要升级**，否则池化行为变化导致与索引失配，检索质量静默劣化）。

### 结构化索引（规格 / 经销商）
- 规格：`spec_index.json`（字段名 → 变体）；`FIELD_ALIAS` 多语言别名字段匹配。
- FAQ：内存列表 + `tokenize`（关键词 score）。
- 经销商：`dealers.jsonl` 内存列表；名称/地址/城市文本匹配（城市别名 `CITY_ALIASES`）+ 坐标最近（`haversine`）。
- 规格/经销商**不使用向量**。

---

## 5. 上下文方案（给 LLM 的 context 串）

`kb.context()` 返回一个**字符串**，拼接顺序：
1. **向量 top-k**（文档+FAQ）：每条 `text` 截断 **500 字符**。
2. **结构化规格**：7 个概念（`range`/`battery`/`power`/`seats`/`dimensions`/`ac-charging`/`dc-charging`）固定追加，字段值带**语言标签**（zh/es 本地化，en/th 用原始名）。
3. **经销商注入**：`dealer_context`（名称/地址/**电话**/距离），按 坐标最近 + 名称·城市别名匹配。
4. **FAQ 关键词兜底**：向量未命中时。

**LLM 完整输入** =
`系统提示词 + state_desc(卡片步骤描述) + 最近 4 轮对话历史 + 上面这份上下文 + 当前消息`。

---

## 6. 一句话总结

> 原始资料走 **ETL** 变结构化 JSONL；**文档按页切、FAQ 单位切**；**文档/FAQ 用 e5-large 向量语义检索，规格用精确字段查询，经销商用名称/城市/坐标匹配**；检索与生成在**一次 LLM 调用**内完成；回复用**下拉框语言**；上下文 = 向量 top-k + 结构化规格 + 经销商注入 + 最近历史。
