# 会话策略（as-built + 非匿名设计）

> 说明：**现状是"匿名会话"**；本文也给出**非匿名（已登录/有档案）用户**的设计增量。二者可共存（未登录走匿名，登录走非匿名）。

---

## 1. 会话模型（`state.Session`）
每个对话 = 一个 `Session`，字段：
- `id`（匿名 `sess_xxxx`）
- `market` / `market_source`（auto|manual）
- `language`
- `intent`（已锁定的当前意图）、`intent_main`、`intent_secondary`、`confidence`
- `step_index`（卡片步骤）、`collected{phone,email,model,concern}`
- `step_asks`（槽位追问计数）、`clarify_rounds`、`total_rounds`
- `escalated` / `resolved` / `target_reached`
- `lead_id` / `rescue_ticket_id` / `lead_record`
- `history`（近几轮，供 LLM 上下文）
- `anonymous`（默认 True）

**存取**：`StateStore` → `data/sessions/<id>.json`；前端带 `sessionId`，每轮加载/保存（断点续聊）。

---

## 2. 匿名会话（现状）
- 无真实身份，随机 `sess_xxxx`。
- 对话中**现收**联系方式（contact 卡）→ 生成**新 lead_record**。
- 线索/工单**无归属**（只有 sessionId/market）。
- 适合未登录的访客/潜客。

**行为**（已实现）：
- 意图：首答 `lock_intent` → 不同业务意图 `switch_intent` → 延续标签保留当前 → 紧急强制。
- 卡片：按意图走状态机（`expect` 校验 → `advance`/`goal` → 留资/救援）。
- 语言：每轮跟下拉框（lang_hint）；市场：auto/manual 粘性。
- 升级：澄清超限 / 槽位超 SLOT_MAX_ASK / 情绪超标。

---

## 3. 非匿名会话（设计增量）
| 维度 | 匿名（现状） | 非匿名（设计） |
|---|---|---|
| 身份 | 随机 sess_xxxx | **`user_id`**（登录/令牌） |
| 续聊 | 按 sessionId | **跨设备/会话找回**（按 `user_id` 聚合） |
| 个人化 | 无档案 | **加载用户档案**：已购车型/联系方式/保修保养 |
| 重问 | 每次收集 → 新线索 | **已知用户跳过重问**，电话/邮箱预填/直接用 |
| 记录归属 | 无归属 | **线索/工单挂 `user_id`** |
| 路由 | 纯意图/市场 | 可结合档案（如已知车主 → 走对应售后） |
| 合规 | 每次 consent | 已授权可**复用授权**（仍按 PDPA/Privacy Act） |

### 需要改的
1. **`state.Session`**：加 `user_id`；`anonymous` 由是否带 user_id 决定。
2. **`StateStore`**：支持按 `user_id` 定位/合并会话（多端共享）。
3. **`db`**：`leads / escalations / rescue_tickets` 加 `user_id` 外键。
4. **`pipeline`**：会话恢复后若有 `user_id` → 查客户档案 → 注入上下文/跳过收集/定向路由。
5. **前端/API**：登录后把 `user_id`（或 token）随 `sessionId` 传给 chat；未登录仍匿名。

---

## 4. 会话生命周期（每轮）
```
用户消息 → 会话恢复(按 sessionId/user_id) → 语言/市场/安全
→ 知识上下文 + 近4轮历史 + state_desc → 一次 LLM(意图+回答, 下拉语言)
→ 意图规整(延续/紧急) → 卡片状态机(推进/留资/救援/升级)
→ 话术质量 + 输出过滤/他牌 → 落库 + 持久化 → 响应
```

---

## 5. 前端改造（非匿名）
- **登录/身份入口**：若宿主站点有账号体系 → 聊天窗显示登录态；否则仍匿名。
- **透传 user_id**：chat 请求带 `user_id`（或令牌），未登录传空。
- **个人化展示**：登录用户显示"您好，[称呼]"，可显示其已购车型/会员。
- **留资行为变化**：已登录已知用户 → 不重复索要联系方式；若需更新则预填。
- **会话/历史**：登录用户跨设备能看到历史；匿名用户只在本会话。
```


---

## 6. 会话结束策略

**结束（过期）条件**（满足其一）：
| 类型 | 判定 | 设置 |
|---|---|---|
| 达成终态 | `target_reached`（留资/救援/明确解决） | `ended_reason=goal` |
| 转人工 | `escalated` | `ended_reason=escalated` |
| 用户结束/告别 | 检测 再见/谢谢/结束/bye/goodbye/结束会话 | `ended_reason=goodbye` |
| 空闲过期(TTL) | 会话超过 `TTL_RECENT`（如 30 分钟）无活动 | `ended_reason=idle` |

**行为**：
- 会话标记 `ended=true` + `ended_at` + `ended_reason`。
- **对话中提示**：当轮响应带 `ended:true` + 结束话术（如"本次咨询已结束，感谢使用。如需继续请开启新会话"）；前端据此显示。
- **保留存档**：结束的会话**不清删**，保留在 `data/sessions`（供历史/个人化/审计）。
- **定期清理**：`StateStore.cleanup()` 定期删除**超过归档期限**（建议 3 个月）的会话文件（按 `ended_at`/文件 mtime）。

**新消息**：会话结束后，用户再发消息 → 前端检测到 `ended` → **开启新会话**（新 sessionId），旧会话进存档。

**需确认的常量**：`TTL_RECENT`（空闲多久过期，建议 30 分钟）、`ARCHIVE_DAYS`（存档保留，建议 90 天）、`notice` 话术。
